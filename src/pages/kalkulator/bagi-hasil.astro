---
import Layout from '../../layouts/Layout.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';
import FAQ from '../../components/FAQ.astro';
import AdSlot from '../../components/AdSlot.astro';
import CalculatorCard from '../../components/CalculatorCard.astro';
import { getCalculatorBySlug, getRelatedCalculators } from '../../data/calculators';

const calc = getCalculatorBySlug('bagi-hasil')!;
const related = getRelatedCalculators('bagi-hasil', 3);

const faqs = [
  { question: 'Apa itu akad mudharabah?', answer: 'Mudharabah adalah <strong>akad kerja sama usaha</strong> antara pemilik modal (<em>shahibul maal</em>) dan pengelola (<em>mudharib</em>). Pemodal menyediakan dana, pengelola menjalankan usaha. Keuntungan dibagi sesuai <strong>nisbah</strong> (rasio bagi hasil) yang disepakati di awal, misalnya 60:40. Bila terjadi kerugian (bukan karena kelalaian pengelola), kerugian finansial ditanggung pemodal, sementara pengelola kehilangan tenaga & waktunya.' },
  { question: 'Apa itu nisbah bagi hasil?', answer: 'Nisbah adalah <strong>rasio pembagian keuntungan</strong> antara pihak-pihak yang bekerja sama, dinyatakan dalam persentase (mis. 60% pemodal : 40% pengelola). Total nisbah harus 100%. Penting: bagi hasil dihitung dari <strong>keuntungan (laba)</strong>, bukan dari omzet atau modal. Nisbah tidak boleh ditetapkan dalam nominal tetap — harus persentase agar sesuai syariah.' },
  { question: 'Bagaimana cara menghitung bagi hasil usaha?', answer: 'Sederhana: <strong>Bagian Pemodal = Laba × Nisbah Pemodal%</strong>, dan <strong>Bagian Pengelola = Laba × Nisbah Pengelola%</strong>. Contoh: laba usaha Rp 20 juta dengan nisbah 60:40 → pemodal dapat Rp 12 juta, pengelola Rp 8 juta. Kalkulator juga menghitung return pemodal terhadap modal yang ditanam.' },
  { question: 'Bagaimana bagi hasil deposito syariah dihitung?', answer: 'Deposito syariah memakai akad mudharabah — kamu (nasabah) jadi pemodal, bank jadi pengelola. Bank biasanya mempublikasikan <strong>indikasi imbal hasil setara (% per tahun)</strong>. Estimasi bagi hasil = Nominal × imbal hasil% × (tenor bulan ÷ 12). Berbeda dengan bunga, angka ini <strong>tidak dijamin tetap</strong> — naik-turun mengikuti kinerja bank, dan bebas riba.' },
  { question: 'Apa beda mudharabah dan musyarakah?', answer: 'Pada <strong>mudharabah</strong>, modal 100% dari pemodal; pengelola hanya menyumbang keahlian/tenaga. Pada <strong>musyarakah</strong>, kedua pihak sama-sama menyetor modal dan biasanya sama-sama mengelola. Pembagian keuntungan keduanya tetap memakai nisbah persentase yang disepakati. Kalkulator ini cocok untuk skema bagi hasil keduanya selama pembagian berdasar nisbah laba.' },
];

const bcItems = [{ label: 'Beranda', href: '/' }, { label: 'Kalkulator', href: '/#tools' }, { label: calc.shortTitle }];
---

<Layout title={calc.title} description={calc.metaDescription} keywords={calc.keywords} canonical={`https://hitungcerdas.net/kalkulator/${calc.slug}`}>
  <section class="calc-hero" style="--cat-color:#CA8A04;--cat-color2:#A16207">
    <div class="wrap">
      <Breadcrumb items={bcItems} />
      <div class="calc-hero-grid">
        <div class="calc-hero-text">
          <div class="calc-hero-tag" style="background:#FEF9C3;color:#854D0E"><i class="ti ti-arrows-split-2"></i> {calc.categoryLabel}</div>
          <h1 class="calc-hero-title">{calc.h1}</h1>
          <p class="calc-hero-sub">{calc.introText}</p>
          <ul class="calc-hero-benefits">{calc.benefits.map(b => <li><i class="ti ti-check"></i> {b}</li>)}</ul>
        </div>
        <div class="calc-hero-illust">
          <svg viewBox="0 0 200 200" fill="none">
            <circle cx="100" cy="100" r="85" fill="rgba(255,255,255,.08)"/>
            <circle cx="100" cy="74" r="20" fill="#FFF" stroke="#FDE047" stroke-width="2"/>
            <text x="92" y="81" fill="#CA8A04" font-size="18" font-weight="bold" font-family="sans-serif">Rp</text>
            <path d="M100 94 V112 M100 112 L66 138 M100 112 L134 138" stroke="#FDE047" stroke-width="4" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
            <circle cx="60" cy="146" r="13" fill="#FFF" stroke="#FDE047" stroke-width="2"/>
            <circle cx="140" cy="146" r="13" fill="#FFF" stroke="#FDE047" stroke-width="2"/>
            <text x="54" y="151" fill="#CA8A04" font-size="12" font-weight="bold" font-family="sans-serif">60</text>
            <text x="134" y="151" fill="#CA8A04" font-size="12" font-weight="bold" font-family="sans-serif">40</text>
          </svg>
        </div>
      </div>
    </div>
  </section>

  <AdSlot format="horizontal" />

  <section class="calc-section">
    <div class="wrap">
      <div class="calc-card">
        <div class="calc-card-hdr">
          <div class="calc-card-hdr-ic" style="background:linear-gradient(135deg,#CA8A04 0%,#A16207 100%)"><i class="ti ti-arrows-split-2"></i></div>
          <div><h2 class="calc-card-title">Kalkulator Bagi Hasil Mudharabah</h2><p class="calc-card-sub">Pembagian laba usaha & estimasi deposito syariah</p></div>
        </div>

        <div class="calc-grid">
          <div class="calc-input">
            <div class="field">
              <label>Jenis Perhitungan</label>
              <select id="mode">
                <option value="usaha" selected>Bagi Hasil Usaha (Kemitraan)</option>
                <option value="deposito">Deposito Syariah (Indikasi)</option>
              </select>
            </div>

            <!-- MODE USAHA -->
            <div id="grpUsaha">
              <div class="field"><label>Modal Usaha (Pemodal) <span class="hint">(opsional)</span></label>
                <div class="input-wrap"><span class="input-prefix">Rp</span>
                  <input type="text" id="modal" class="has-prefix" value="100.000.000" inputmode="numeric" /></div>
                <p class="hint" style="margin-top:6px">Untuk menghitung return pemodal terhadap modal.</p>
              </div>
              <div class="field"><label>Keuntungan / Laba Usaha</label>
                <div class="input-wrap"><span class="input-prefix">Rp</span>
                  <input type="text" id="laba" class="has-prefix" value="20.000.000" inputmode="numeric" /></div>
                <p class="hint" style="margin-top:6px">Laba bersih usaha (bukan omzet). Bagi hasil dihitung dari laba.</p>
              </div>
              <div class="field"><label>Nisbah Pemodal (Shahibul Maal)</label>
                <div class="input-wrap"><span class="input-suffix">%</span>
                  <input type="number" id="nisbah" class="has-suffix" value="60" min="1" max="99" step="1" /></div>
                <p class="hint" style="margin-top:6px">Bagian pengelola otomatis = sisanya. Contoh 60 berarti 60:40.</p>
              </div>
            </div>

            <!-- MODE DEPOSITO -->
            <div id="grpDeposito" style="display:none">
              <div class="field"><label>Nominal Deposito / Tabungan</label>
                <div class="input-wrap"><span class="input-prefix">Rp</span>
                  <input type="text" id="deposito" class="has-prefix" value="50.000.000" inputmode="numeric" /></div>
              </div>
              <div class="field"><label>Indikasi Imbal Hasil Setara</label>
                <div class="input-wrap"><span class="input-suffix">% / thn</span>
                  <input type="number" id="rate" class="has-suffix" value="4.5" min="0" max="15" step="0.1" /></div>
                <p class="hint" style="margin-top:6px">Indikasi return setara yang dipublikasikan bank syariah. Tidak dijamin tetap.</p>
              </div>
              <div class="field"><label>Tenor</label>
                <div class="input-wrap"><span class="input-suffix">bulan</span>
                  <input type="number" id="tenor" class="has-suffix" value="12" min="1" max="120" step="1" /></div>
              </div>
            </div>

            <div class="calc-action-bar">
              <button type="button" class="btn-hitung" id="btnHitung"><i class="ti ti-calculator"></i> Hitung Sekarang</button>
              <button type="button" class="btn-reset" id="btnReset"><i class="ti ti-refresh"></i> Reset</button>
            </div>
          </div>

          <div class="calc-output">
            <div class="result-placeholder" id="placeholder">
              <i class="ti ti-arrows-split-2 result-placeholder-icon"></i>
              <div class="result-placeholder-title">Bagi hasil akan muncul di sini</div>
              <div class="result-placeholder-desc">Pilih jenis perhitungan, isi data, klik <strong>"Hitung Sekarang"</strong>.</div>
            </div>

            <div class="result-wrapper" id="results">
              <div class="result-main">
                <div class="result-label" id="mainLabel">Bagian Pemodal</div>
                <div class="result-value" id="mainValue">Rp 0</div>
                <div class="result-meta"><i class="ti ti-info-circle"></i> <span id="mainMeta"></span></div>
              </div>

              <div class="result-grid" id="gridUsaha">
                <div class="result-item"><div class="result-item-label">Bagian Pemodal</div><div class="result-item-value" id="bagPemodal" style="color:var(--green-dark)">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">Bagian Pengelola</div><div class="result-item-value" id="bagPengelola">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">Nisbah</div><div class="result-item-value" id="nisbahOut">0:0</div></div>
                <div class="result-item"><div class="result-item-label">Return Pemodal</div><div class="result-item-value" id="returnPemodal">—</div></div>
              </div>

              <div class="result-grid" id="gridDeposito" style="display:none">
                <div class="result-item"><div class="result-item-label">Estimasi Bagi Hasil</div><div class="result-item-value" id="depBagiHasil" style="color:var(--green-dark)">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">Total Saat Jatuh Tempo</div><div class="result-item-value" id="depTotal">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">Per Bulan (rata-rata)</div><div class="result-item-value" id="depBulan">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">Tenor</div><div class="result-item-value" id="depTenor">0 bln</div></div>
              </div>

              <div id="bhInfo" style="margin-top:10px;padding:11px 13px;border-radius:12px;background:#FEFCE8;border:1px solid #FEF08A;font-size:12.5px;line-height:1.55;color:#854D0E"></div>

              <div id="v2Usaha" style="display:none">
                <div style="margin-top:12px">
                  <div style="font-size:12px;font-weight:600;color:var(--text-soft);margin-bottom:6px">Pembagian Laba</div>
                  <div style="display:flex;height:22px;border-radius:6px;overflow:hidden;font-size:10px;font-weight:700;color:#fff">
                    <div id="barPemodal" style="background:#CA8A04;display:flex;align-items:center;justify-content:center;width:50%">Pemodal</div>
                    <div id="barPengelola" style="background:#0F766E;display:flex;align-items:center;justify-content:center;width:50%">Pengelola</div>
                  </div>
                  <div style="display:flex;justify-content:space-between;font-size:11px;color:var(--text-soft);margin-top:4px">
                    <span id="lblBarPemodal">Pemodal 0%</span><span id="lblBarPengelola">Pengelola 0%</span>
                  </div>
                </div>
                <div style="margin-top:12px">
                  <div style="font-size:12.5px;font-weight:700;color:var(--text);margin-bottom:7px"><i class="ti ti-table"></i> Simulasi Beberapa Nisbah</div>
                  <div style="overflow-x:auto">
                    <table class="compare-table" style="width:100%;font-size:12px">
                      <thead><tr><th>Nisbah (Pemodal:Pengelola)</th><th class="right">Pemodal</th><th class="right">Pengelola</th></tr></thead>
                      <tbody id="nisbahBody"></tbody>
                    </table>
                  </div>
                  <p class="hint" style="margin-top:6px">Baris <b>★</b> = nisbah pilihanmu. Bantu menimbang pembagian yang adil saat akad.</p>
                </div>
              </div>

              <div id="v2Deposito" style="display:none">
                <div style="margin-top:12px">
                  <div style="font-size:12.5px;font-weight:700;color:var(--text);margin-bottom:7px"><i class="ti ti-calendar-stats"></i> Proyeksi Bagi Hasil per Tenor</div>
                  <div style="overflow-x:auto">
                    <table class="compare-table" style="width:100%;font-size:12px">
                      <thead><tr><th>Tenor</th><th class="right">Estimasi Bagi Hasil</th><th class="right">Total Jatuh Tempo</th></tr></thead>
                      <tbody id="tenorBody"></tbody>
                    </table>
                  </div>
                  <p class="hint" style="margin-top:6px">Baris <b>★</b> = tenor pilihanmu. Indikasi setara — tidak dijamin tetap.</p>
                </div>
              </div>

              <div class="export-row">
                <button type="button" class="export-btn img" id="exportImg"><i class="ti ti-photo-down"></i><span>Save Image</span></button>
                <button type="button" class="export-btn wa" id="exportWa"><i class="ti ti-brand-whatsapp"></i><span>Share WA</span></button>
                <button type="button" class="export-btn pdf" id="exportPdf"><i class="ti ti-printer"></i><span>Print/PDF</span></button>
                <button type="button" class="export-btn copy" id="exportCopy"><i class="ti ti-copy"></i><span>Copy Teks</span></button>
                <button type="button" class="export-btn" id="exportExcel"><i class="ti ti-file-spreadsheet" style="color:#1D6F42"></i><span style="color:#1D6F42">Excel</span></button>
              </div>
              <!-- ===== AI COACH BAGI HASIL ===== -->
              <div class="aic-wrap" id="aicWrap">
                <button type="button" class="aic-trigger" id="aicTrigger">
                  <span class="aic-trigger-ic"><i class="ti ti-sparkles"></i></span>
                  <span class="aic-trigger-txt">
                    <strong>Tanya AI Coach</strong>
                    <small>Bingung nisbah/bagi hasil? Tanya di sini</small>
                  </span>
                  <i class="ti ti-chevron-down aic-trigger-arr"></i>
                </button>
                <div class="aic-panel" id="aicPanel" hidden>
                  <div class="aic-head">
                    <span class="aic-head-ic"><i class="ti ti-sparkles"></i></span>
                    <div class="aic-head-tt">
                      <strong>AI Coach Syariah</strong>
                      <small>Bantu pahami bagi hasil-mu</small>
                    </div>
                    <span class="aic-badge">BETA</span>
                  </div>
                  <div class="aic-ground" id="aicGround"></div>
                  <div class="aic-msgs" id="aicMsgs"></div>
                  <div class="aic-chips" id="aicChips"></div>
                  <div class="aic-input">
                    <input type="text" id="aicText" placeholder="Tanya soal bagi hasil syariah..." maxlength="600" autocomplete="off" />
                    <button type="button" id="aicSend" aria-label="Kirim"><i class="ti ti-arrow-up"></i></button>
                  </div>
                  <div class="aic-disc">
                    <i class="ti ti-info-circle"></i>
                    <span>Penjelasan edukatif berbasis hitunganmu, bukan nasihat keuangan/fatwa resmi.</span>
                  </div>
                  <div class="aic-limit" id="aicLimit"></div>
                </div>
              </div>
              <!-- ===== /AI COACH ===== -->


            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="content-section">
    <div class="wrap-text">
      <h2>Memahami Bagi Hasil Mudharabah</h2>
      <p>Bagi hasil adalah jantung ekonomi syariah. Alih-alih bunga (yang dianggap riba), keuntungan dibagi berdasarkan <strong>nisbah</strong> — rasio yang disepakati di awal. Prinsip ini dipakai dalam dua konteks utama: kemitraan usaha (mudharabah/musyarakah) dan produk simpanan bank syariah (deposito/tabungan mudharabah).</p>

      <h3>Akad Mudharabah</h3>
      <p>Mudharabah mempertemukan <strong>pemodal (shahibul maal)</strong> yang menyediakan dana dengan <strong>pengelola (mudharib)</strong> yang menjalankan usaha. Keuntungan dibagi sesuai nisbah, misalnya 60:40. Jika usaha rugi tanpa kelalaian pengelola, kerugian finansial ditanggung pemodal — pengelola sudah rugi tenaga dan waktu. Inilah keadilan risiko dalam syariah.</p>

      <h3>Rumus</h3>
      <ul>
        <li><strong>Bagian Pemodal</strong> = Laba × Nisbah Pemodal%</li>
        <li><strong>Bagian Pengelola</strong> = Laba × Nisbah Pengelola%</li>
        <li><strong>Deposito syariah</strong>: Bagi Hasil = Nominal × imbal hasil setara% × (tenor ÷ 12)</li>
      </ul>

      <h3>Catatan Penting</h3>
      <p>Dua hal yang sering keliru: <strong>pertama</strong>, bagi hasil dihitung dari laba/keuntungan, bukan dari omzet atau modal. <strong>Kedua</strong>, nisbah harus berupa persentase, bukan nominal tetap — menetapkan "pengelola dapat Rp X tetap" justru menyalahi prinsip syariah karena menyerupai bunga. Untuk deposito syariah, angka imbal hasil bersifat indikasi dan bisa naik-turun mengikuti kinerja bank, berbeda dengan bunga deposito konvensional yang tetap.</p>

      <div class="cta-box">
        <h3>Disclaimer</h3>
        <p>Kalkulator ini alat bantu simulasi &amp; edukasi. Untuk deposito syariah, hasil adalah <strong>estimasi berdasarkan indikasi imbal hasil</strong> yang kamu masukkan dan tidak dijamin — realisasi bergantung kinerja bank. Pembagian bagi hasil usaha mengikuti kesepakatan akad yang sah. Bukan nasihat keuangan; konsultasikan dengan lembaga keuangan syariah atau ahli muamalah untuk keputusan yang mengikat.</p>
      </div>
    </div>
  </section>

  <AdSlot format="horizontal" />
  <FAQ title="Pertanyaan Seputar Bagi Hasil Syariah" items={faqs} />

  <section class="section"><div class="wrap">
    <div class="section-hdr"><div class="section-eyebrow">Kalkulator Lainnya</div><h2 class="section-title">Hitung Juga yang Lain</h2></div>
    <div class="related-grid">{related.map(c => <CalculatorCard calc={c} />)}</div>
  </div></section>

  <p class="hc-method-note" style="text-align:center;font-size:.85rem;color:#64748b;margin:1.2rem auto;max-width:680px;line-height:1.6;padding:0 1rem;">
    📅 Diperbarui <strong>Juni 2026</strong> · Memakai prinsip bagi hasil akad mudharabah/musyarakah sesuai nisbah yang disepakati, bukan bunga tetap. Estimasi edukatif. <strong>Sumber:</strong> <a href="https://dsnmui.or.id/" target="_blank" rel="noopener">DSN-MUI</a> &amp; <a href="https://www.ojk.go.id/" target="_blank" rel="noopener">OJK</a>.
  </p>

<script is:inline>
  (function() {
    function ready(fn) { if (window.HC && window.html2canvas) fn(); else setTimeout(() => ready(fn), 100); }
    ready(() => {
      const $ = id => document.getElementById(id);
      const els = { mode: $('mode'), modal: $('modal'), laba: $('laba'), nisbah: $('nisbah'), deposito: $('deposito'), rate: $('rate'), tenor: $('tenor') };
      [els.modal, els.laba, els.deposito].forEach(el => el.addEventListener('input', HC.fmtInput));

      function toggleMode() {
        const usaha = els.mode.value === 'usaha';
        $('grpUsaha').style.display = usaha ? '' : 'none';
        $('grpDeposito').style.display = usaha ? 'none' : '';
        if (lastCalc) hitung();
      }
      els.mode.addEventListener('change', toggleMode);

      let lastCalc = null;

      function hitung() {
        if (els.mode.value === 'usaha') hitungUsaha();
        else hitungDeposito();
      }

      function hitungUsaha() {
        const modal = HC.parseRp(els.modal.value);
        const laba = HC.parseRp(els.laba.value);
        const nisbah = parseFloat(els.nisbah.value) || 0;
        if (laba === 0) { HC.toast('Masukkan laba usaha dulu', 'error'); return; }
        if (nisbah < 1 || nisbah > 99) { HC.toast('Nisbah pemodal antara 1-99%', 'error'); return; }

        const nisbahPengelola = 100 - nisbah;
        const bagPemodal = laba * (nisbah / 100);
        const bagPengelola = laba * (nisbahPengelola / 100);
        const returnPct = modal > 0 ? bagPemodal / modal * 100 : null;

        $('mainLabel').textContent = 'Bagian Pemodal';
        $('mainValue').textContent = HC.fmt(bagPemodal);
        $('mainMeta').textContent = `nisbah ${nisbah}:${nisbahPengelola} dari laba ${HC.fmt(laba)}`;
        $('gridUsaha').style.display = '';
        $('gridDeposito').style.display = 'none';
        $('bagPemodal').textContent = HC.fmt(bagPemodal);
        $('bagPengelola').textContent = HC.fmt(bagPengelola);
        $('nisbahOut').textContent = `${nisbah} : ${nisbahPengelola}`;
        $('returnPemodal').textContent = returnPct !== null ? returnPct.toFixed(1) + '%' : '\u2014';

        $('bhInfo').innerHTML = `\ud83e\udd1d Dari laba <b>${HC.fmt(laba)}</b> dengan nisbah <b>${nisbah}:${nisbahPengelola}</b> \u2014 pemodal menerima <b>${HC.fmt(bagPemodal)}</b>, pengelola <b>${HC.fmt(bagPengelola)}</b>.${returnPct !== null ? ` Return pemodal terhadap modal ${HC.fmt(modal)}: <b>${returnPct.toFixed(1)}%</b>.` : ''}`;

        // V2: bar pembagian + simulasi nisbah
        $('v2Usaha').style.display = '';
        $('v2Deposito').style.display = 'none';
        $('barPemodal').style.width = nisbah + '%';
        $('barPengelola').style.width = nisbahPengelola + '%';
        $('lblBarPemodal').textContent = 'Pemodal ' + nisbah + '%';
        $('lblBarPengelola').textContent = 'Pengelola ' + nisbahPengelola + '%';
        var nPre = [50, 60, 70, 80];
        if (nPre.indexOf(nisbah) === -1) nPre.push(nisbah);
        nPre.sort(function(a, b) { return a - b; });
        var nHtml = '';
        nPre.forEach(function(n) {
          var pm = laba * n / 100, pg = laba * (100 - n) / 100;
          var isT = (n === nisbah);
          nHtml += '<tr' + (isT ? ' style="background:#FEFCE8;font-weight:700"' : '') + '><td>' + (isT ? '\u2605 ' : '') + n + ' : ' + (100 - n) + '</td><td class="right" style="color:var(--green-dark)">' + HC.fmt(pm) + '</td><td class="right">' + HC.fmt(pg) + '</td></tr>';
        });
        $('nisbahBody').innerHTML = nHtml;

        lastCalc = { mode: 'usaha', modal, laba, nisbah, nisbahPengelola, bagPemodal, bagPengelola, returnPct };
        showResults();
      }

      function hitungDeposito() {
        const deposito = HC.parseRp(els.deposito.value);
        const rate = parseFloat(els.rate.value) || 0;
        const tenor = parseInt(els.tenor.value) || 0;
        if (deposito === 0) { HC.toast('Masukkan nominal deposito dulu', 'error'); return; }
        if (tenor < 1) { HC.toast('Tenor minimal 1 bulan', 'error'); return; }

        const bagiHasil = deposito * (rate / 100) * (tenor / 12);
        const total = deposito + bagiHasil;
        const perBulan = bagiHasil / tenor;

        $('mainLabel').textContent = 'Estimasi Bagi Hasil';
        $('mainValue').textContent = HC.fmt(bagiHasil);
        $('mainMeta').textContent = `indikasi setara ${rate}%/thn \u00b7 ${tenor} bulan`;
        $('gridUsaha').style.display = 'none';
        $('gridDeposito').style.display = '';
        $('depBagiHasil').textContent = HC.fmt(bagiHasil);
        $('depTotal').textContent = HC.fmt(total);
        $('depBulan').textContent = HC.fmt(perBulan);
        $('depTenor').textContent = tenor + ' bln';

        $('bhInfo').innerHTML = `\ud83c\udfe6 Deposito <b>${HC.fmt(deposito)}</b> dengan indikasi imbal hasil setara <b>${rate}%/thn</b> selama ${tenor} bulan \u2192 estimasi bagi hasil <b>${HC.fmt(bagiHasil)}</b>, total jatuh tempo <b>${HC.fmt(total)}</b>. Ingat: angka ini indikasi (tidak dijamin tetap), beda dengan bunga konvensional.`;

        // V2: proyeksi multi-tenor
        $('v2Deposito').style.display = '';
        $('v2Usaha').style.display = 'none';
        var tPre = [3, 6, 12, 24];
        if (tPre.indexOf(tenor) === -1) tPre.push(tenor);
        tPre.sort(function(a, b) { return a - b; });
        var tHtml = '';
        tPre.forEach(function(t) {
          var bh = deposito * (rate / 100) * (t / 12), tot = deposito + bh;
          var isT = (t === tenor);
          tHtml += '<tr' + (isT ? ' style="background:#FEFCE8;font-weight:700"' : '') + '><td>' + (isT ? '\u2605 ' : '') + t + ' bln</td><td class="right" style="color:var(--green-dark)">' + HC.fmt(bh) + '</td><td class="right">' + HC.fmt(tot) + '</td></tr>';
        });
        $('tenorBody').innerHTML = tHtml;

        lastCalc = { mode: 'deposito', deposito, rate, tenor, bagiHasil, total, perBulan };
        showResults();
      }

      function showResults() {
        $('placeholder').style.display = 'none';
        $('results').classList.add('show');
        $('results').scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        HC.toast('Berhasil dihitung!');
      }

      function reset() {
        els.mode.value = 'usaha';
        els.modal.value = '100.000.000'; els.laba.value = '20.000.000'; els.nisbah.value = '60';
        els.deposito.value = '50.000.000'; els.rate.value = '4.5'; els.tenor.value = '12';
        toggleMode();
        $('placeholder').style.display = ''; $('results').classList.remove('show');
        lastCalc = null; HC.toast('Form direset', 'info');
      }

      function buildReport() {
        if (!lastCalc) return null;
        const c = lastCalc;
        if (c.mode === 'usaha') {
          const insight = `Akad mudharabah. Laba ${HC.fmt(c.laba)}, nisbah ${c.nisbah}:${c.nisbahPengelola}. Pemodal ${HC.fmt(c.bagPemodal)}, pengelola ${HC.fmt(c.bagPengelola)}.${c.returnPct !== null ? ` Return pemodal ${c.returnPct.toFixed(1)}%.` : ''} Bagi hasil dari laba, bukan omzet. Edukatif.`;
          return HC.buildReportCard({
            title: '\ud83e\udd1d Bagi Hasil Usaha (Mudharabah)',
            color: '#CA8A04', color2: '#A16207',
            heroLabel: 'Bagian Pemodal',
            heroValue: HC.fmt(c.bagPemodal),
            heroSubtext: `nisbah ${c.nisbah}:${c.nisbahPengelola} dari laba ${HC.fmt(c.laba)}`,
            inputs: [
              { label: '\ud83d\udcb5 Modal Usaha', value: HC.fmt(c.modal) },
              { label: '\ud83d\udcc8 Laba Usaha', value: HC.fmt(c.laba) },
              { label: '\u2696\ufe0f Nisbah', value: c.nisbah + ' : ' + c.nisbahPengelola },
            ],
            results: [
              { label: 'Bagian Pemodal', value: HC.fmt(c.bagPemodal), highlight: true },
              { label: 'Bagian Pengelola', value: HC.fmt(c.bagPengelola), highlight: true },
              { label: 'Return Pemodal', value: c.returnPct !== null ? c.returnPct.toFixed(1) + '%' : '\u2014' },
            ],
            insight, slug: 'bagi-hasil',
          });
        } else {
          const insight = `Deposito syariah ${HC.fmt(c.deposito)}, indikasi imbal hasil setara ${c.rate}%/thn, tenor ${c.tenor} bulan. Estimasi bagi hasil ${HC.fmt(c.bagiHasil)}, total ${HC.fmt(c.total)}. Indikasi, tidak dijamin tetap.`;
          return HC.buildReportCard({
            title: '\ud83c\udfe6 Deposito Syariah (Mudharabah)',
            color: '#CA8A04', color2: '#A16207',
            heroLabel: 'Estimasi Bagi Hasil',
            heroValue: HC.fmt(c.bagiHasil),
            heroSubtext: `setara ${c.rate}%/thn \u00b7 ${c.tenor} bulan`,
            inputs: [
              { label: '\ud83c\udfe6 Nominal Deposito', value: HC.fmt(c.deposito) },
              { label: '\ud83d\udcca Imbal Hasil Setara', value: c.rate + '%/thn' },
              { label: '\u23f3 Tenor', value: c.tenor + ' bulan' },
            ],
            results: [
              { label: 'Estimasi Bagi Hasil', value: HC.fmt(c.bagiHasil), highlight: true },
              { label: 'Total Jatuh Tempo', value: HC.fmt(c.total), highlight: true },
              { label: 'Per Bulan (rata2)', value: HC.fmt(c.perBulan) },
            ],
            insight, slug: 'bagi-hasil',
          });
        }
      }

      function summaryText() {
        if (!lastCalc) return '';
        const c = lastCalc;
        if (c.mode === 'usaha') {
          return `\ud83e\udd1d *Bagi Hasil Usaha (Mudharabah) - HitungCerdas*\n\nLaba usaha: ${HC.fmt(c.laba)}\nNisbah: ${c.nisbah}:${c.nisbahPengelola}\n\n\ud83d\udcb0 *Pemodal: ${HC.fmt(c.bagPemodal)}*\n\ud83d\udc77 Pengelola: ${HC.fmt(c.bagPengelola)}${c.returnPct !== null ? `\n\ud83d\udcc8 Return pemodal: ${c.returnPct.toFixed(1)}%` : ''}\n\nHitung di: https://hitungcerdas.net/kalkulator/bagi-hasil`;
        }
        return `\ud83c\udfe6 *Deposito Syariah (Mudharabah) - HitungCerdas*\n\nNominal: ${HC.fmt(c.deposito)}\nImbal hasil setara: ${c.rate}%/thn\nTenor: ${c.tenor} bulan\n\n\ud83d\udcb0 *Estimasi bagi hasil: ${HC.fmt(c.bagiHasil)}*\n\ud83c\udfe6 Total jatuh tempo: ${HC.fmt(c.total)}\n(indikasi, tidak dijamin tetap)\n\nHitung di: https://hitungcerdas.net/kalkulator/bagi-hasil`;
      }

      $('btnHitung').addEventListener('click', hitung);
      $('btnReset').addEventListener('click', reset);
      $('exportImg').addEventListener('click', () => { const c = buildReport(); if (!c) { HC.toast('Hitung dulu', 'error'); return; } HC.exportToImage(c, 'bagi-hasil-' + Date.now()); });
      $('exportWa').addEventListener('click', () => { const c = buildReport(); if (!c) { HC.toast('Hitung dulu', 'error'); return; } HC.shareWhatsApp(c, summaryText(), 'bagi-hasil'); });
      $('exportPdf').addEventListener('click', () => { if (!lastCalc) { HC.toast('Hitung dulu', 'error'); return; } HC.exportToPDF(); });
      $('exportCopy').addEventListener('click', () => { if (!lastCalc) { HC.toast('Hitung dulu', 'error'); return; } HC.copyText(summaryText(), 'Tersalin!'); });
      $('exportExcel').addEventListener('click', () => { const c = buildReport(); if (!c) { HC.toast('Hitung dulu', 'error'); return; } HC.exportToExcel(c, 'bagi-hasil-' + Date.now()); });
    });
  })();
</script>
<style is:global>
  .aic-wrap{margin-top:18px}
  .aic-trigger{width:100%;display:flex;align-items:center;gap:12px;background:#FEFCE8;border:1px solid #FDE68A;border-radius:14px;padding:13px 15px;cursor:pointer;text-align:left;transition:background .15s;font-family:inherit}
  .aic-trigger:hover{background:#FEF9C3}
  .aic-trigger-ic{width:36px;height:36px;flex-shrink:0;border-radius:50%;background:#CA8A04;color:#fff;display:flex;align-items:center;justify-content:center;font-size:19px}
  .aic-trigger-txt{flex:1;display:flex;flex-direction:column;line-height:1.25}
  .aic-trigger-txt strong{font-size:14px;color:#0A2540}
  .aic-trigger-txt small{font-size:12px;color:#64748B}
  .aic-trigger-arr{font-size:18px;color:#64748B;transition:transform .2s}
  .aic-wrap.open .aic-trigger-arr{transform:rotate(180deg)}

  .aic-panel{margin-top:10px;background:#fff;border:1px solid #E5EAF1;border-radius:16px;overflow:hidden;animation:aicIn .2s ease}
  @keyframes aicIn{from{opacity:0;transform:translateY(-6px)}to{opacity:1;transform:none}}
  .aic-head{display:flex;align-items:center;gap:10px;padding:12px 14px;border-bottom:1px solid #E5EAF1}
  .aic-head-ic{width:30px;height:30px;border-radius:50%;background:#FEFCE8;color:#CA8A04;display:flex;align-items:center;justify-content:center;font-size:16px}
  .aic-head-tt{flex:1;display:flex;flex-direction:column;line-height:1.2}
  .aic-head-tt strong{font-size:14px;color:#0A2540}
  .aic-head-tt small{font-size:11px;color:#64748B}
  .aic-badge{font-size:10px;font-weight:700;background:#FEFCE8;color:#CA8A04;padding:2px 8px;border-radius:8px}

  .aic-ground{padding:12px 14px;border-bottom:1px solid #E5EAF1;background:#F8FAFC}
  .aic-ctx{font-size:12px;color:#475569;margin-bottom:10px}
  .aic-ctx b{color:#0A2540;font-weight:700}
  .aic-gauge{position:relative;padding-top:19px}
  .aic-gauge-mark{position:absolute;top:0;z-index:3}
  .aic-gauge-mark span{display:block;font-size:10px;font-weight:700;background:#0A2540;color:#fff;padding:1px 7px;border-radius:9px;white-space:nowrap;transform:translateX(-50%)}
  .aic-gauge-mark::after{content:"";position:absolute;left:0;top:15px;width:2px;height:15px;background:#0A2540}
  .aic-gauge-bar{display:flex;height:10px;border-radius:5px;overflow:hidden}
  .aic-gauge-bar .z-green{flex:0 0 60%;background:#D1FAE5}
  .aic-gauge-bar .z-amber{flex:0 0 20%;background:#FEF3C7}
  .aic-gauge-bar .z-red{flex:0 0 20%;background:#FEE2E2}
  .aic-gauge-lbl{display:flex;font-size:10px;margin-top:5px}
  .aic-gauge-lbl .g{flex:0 0 60%;color:#059669}
  .aic-gauge-lbl .a{flex:0 0 20%;text-align:center;color:#D97706;font-weight:600}
  .aic-gauge-lbl .r{flex:0 0 20%;text-align:right;color:#DC2626}
  .aic-verdict{font-size:12.5px;font-weight:600;margin-top:11px;display:flex;align-items:center;gap:6px}
  .aic-verdict.good{color:#059669}
  .aic-verdict.warn{color:#D97706}
  .aic-verdict.bad{color:#DC2626}
  .aic-nogauge{font-size:11px;color:#94A3B8;padding:4px 0}

  .aic-msgs{padding:12px 14px;display:flex;flex-direction:column;gap:10px;max-height:320px;overflow-y:auto}
  .aic-msgs:empty{display:none}
  .aic-row{display:flex;gap:8px;align-items:flex-start}
  .aic-row.user{justify-content:flex-end}
  .aic-av{width:24px;height:24px;flex-shrink:0;border-radius:50%;background:#FEFCE8;color:#CA8A04;display:flex;align-items:center;justify-content:center;font-size:13px}
  .aic-bub{max-width:82%;font-size:13px;line-height:1.55;border-radius:13px;padding:9px 12px;white-space:pre-wrap;word-wrap:break-word;overflow-wrap:anywhere}
  .aic-row.ai .aic-bub{background:#F1F5F9;color:#0A2540}
  .aic-row.user .aic-bub{background:#CA8A04;color:#fff}
  .aic-bub.aic-rich{white-space:normal}
  .aic-bub.aic-rich p{margin:0 0 7px}
  .aic-bub.aic-rich p:last-child{margin-bottom:0}
  .aic-bub.aic-rich p.aic-h{font-weight:700;margin:10px 0 5px}
  .aic-bub.aic-rich p.aic-h:first-child{margin-top:0}
  .aic-bub.aic-rich strong{font-weight:700}
  .aic-bub.aic-rich em{font-style:italic}
  .aic-bub.aic-rich code{background:rgba(0,0,0,.06);padding:1px 5px;border-radius:4px;font-size:12px}
  .aic-bub.aic-rich .aic-li{display:flex;gap:7px;margin:4px 0;align-items:flex-start}
  .aic-bub.aic-rich .aic-li:first-child{margin-top:0}
  .aic-bub.aic-rich .aic-mk{flex-shrink:0;color:#CA8A04;font-weight:700;min-width:15px}
  .aic-typing span{display:inline-block;width:6px;height:6px;border-radius:50%;background:#94A3B8;margin:0 1px;animation:aicblink 1.2s infinite}
  .aic-typing span:nth-child(2){animation-delay:.2s}
  .aic-typing span:nth-child(3){animation-delay:.4s}
  @keyframes aicblink{0%,60%,100%{opacity:.3}30%{opacity:1}}
  @keyframes aicIn{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
  .aic-row{animation:aicIn .25s ease both}
  .aic-copy{margin-top:8px;display:inline-flex;align-items:center;gap:5px;font-size:11px;color:#64748B;background:#fff;border:1px solid #E2E8F0;border-radius:7px;padding:3px 9px;cursor:pointer;transition:all .15s}
  .aic-copy:hover{color:#A16207;border-color:#A16207;background:#FEFCE8}
  .aic-copy i{font-size:13px}

  .aic-chips{display:flex;flex-wrap:wrap;gap:6px;padding:0 14px 12px}
  .aic-chips:empty{display:none}
  .aic-chip{font-size:11px;background:#FEFCE8;color:#CA8A04;border:1px solid #FDE68A;border-radius:14px;padding:6px 11px;cursor:pointer;font-family:inherit}
  .aic-chip:hover{background:#FEF9C3}

  .aic-input{display:flex;align-items:center;gap:8px;padding:10px 12px;border-top:1px solid #E5EAF1}
  .aic-input input{flex:1;border:1px solid #E5EAF1;border-radius:18px;padding:9px 14px;font-size:13px;background:#F8FAFC;color:#0A2540;outline:none;font-family:inherit}
  .aic-input input:focus{border-color:#CA8A04}
  .aic-input button{width:34px;height:34px;flex-shrink:0;border:0;border-radius:50%;background:#CA8A04;color:#fff;cursor:pointer;display:flex;align-items:center;justify-content:center;font-size:17px}
  .aic-input button:disabled{background:#CBD5E1;cursor:not-allowed}

  .aic-disc{display:flex;gap:6px;align-items:flex-start;padding:8px 14px;font-size:10.5px;color:#94A3B8;line-height:1.45;border-top:1px solid #F1F5F9}
  .aic-disc i{margin-top:1px}
  .aic-limit{padding:0 14px 10px;font-size:11px;color:#64748B}

  /* ===== DARK MODE (mandiri, via .aic-dark) ===== */
  .aic-dark .aic-trigger{background:#16263B;border-color:#2A445F}
  .aic-dark .aic-trigger:hover{background:#1B2E47}
  .aic-dark .aic-trigger-txt strong{color:#E8EEF6}
  .aic-dark .aic-trigger-txt small{color:#93A4B8}
  .aic-dark .aic-trigger-arr{color:#93A4B8}
  .aic-dark .aic-panel{background:#0E1A2B;border-color:#243A56}
  .aic-dark .aic-head{border-color:#243A56}
  .aic-dark .aic-head-ic{background:#172A44;color:#FACC15}
  .aic-dark .aic-head-tt strong{color:#E8EEF6}
  .aic-dark .aic-head-tt small{color:#93A4B8}
  .aic-dark .aic-badge{background:#172A44;color:#FACC15}
  .aic-dark .aic-ground{background:#122036;border-color:#243A56}
  .aic-dark .aic-ctx{color:#A9B7C9}
  .aic-dark .aic-ctx b{color:#E8EEF6}
  .aic-dark .aic-gauge-mark span{background:#E8EEF6;color:#0E1A2B}
  .aic-dark .aic-gauge-mark::after{background:#E8EEF6}
  .aic-dark .aic-gauge-bar .z-green{background:rgba(16,185,129,.24)}
  .aic-dark .aic-gauge-bar .z-amber{background:rgba(245,158,11,.24)}
  .aic-dark .aic-gauge-bar .z-red{background:rgba(239,68,68,.24)}
  .aic-dark .aic-gauge-lbl .g{color:#34D399}
  .aic-dark .aic-gauge-lbl .a{color:#FBBF24}
  .aic-dark .aic-gauge-lbl .r{color:#F87171}
  .aic-dark .aic-verdict.good{color:#34D399}
  .aic-dark .aic-verdict.warn{color:#FBBF24}
  .aic-dark .aic-verdict.bad{color:#F87171}
  .aic-dark .aic-av{background:#172A44;color:#FACC15}
  .aic-dark .aic-row.ai .aic-bub{background:#1B2C44;color:#E8EEF6}
  .aic-dark .aic-bub.aic-rich .aic-mk{color:#FACC15}
  .aic-dark .aic-bub.aic-rich code{background:rgba(255,255,255,.1)}
  .aic-dark .aic-chip{background:rgba(202,138,4,.16);color:#FACC15;border-color:#2A445F}
  .aic-dark .aic-chip:hover{background:rgba(202,138,4,.26)}
  .aic-dark .aic-input{border-color:#243A56}
  .aic-dark .aic-input input{background:#16263B;border-color:#2A445F;color:#E8EEF6}
  .aic-dark .aic-input input:focus{border-color:#EAB308}
  .aic-dark .aic-disc{color:#7E8DA1;border-color:#1B2C44}
  .aic-dark .aic-limit{color:#93A4B8}
</style>
<script is:inline>
  (function(){
    var LIMIT = 10;
    function ready(fn){ if(window.HC){ fn(); } else { setTimeout(function(){ready(fn);},120); } }
    function $(id){ return document.getElementById(id); }
    ready(function(){
      var wrap=$('aicWrap'); if(!wrap) return;
      var trigger=$('aicTrigger'), panel=$('aicPanel'), ground=$('aicGround'), msgs=$('aicMsgs'), chips=$('aicChips');
      var input=$('aicText'), send=$('aicSend'), limitEl=$('aicLimit'); var history=[];
      function isDark(){ var de=document.documentElement; return de.classList.contains('dark')||de.getAttribute('data-theme')==='dark'||(function(){try{return localStorage.getItem('hc-theme')==='dark';}catch(e){return false;}})(); }
      function applyTheme(){ wrap.classList.toggle('aic-dark', isDark()); }
      applyTheme();
      try{ new MutationObserver(applyTheme).observe(document.documentElement,{attributes:true,attributeFilter:['class','data-theme']}); }catch(e){}
      window.addEventListener('storage', function(e){ if(e.key==='hc-theme') applyTheme(); });
      function esc(t){ return String(t).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }

      function readData(){
        var v=function(id){ return ((($(id)||{}).textContent)||'').trim(); };
        var val=function(id){ return ($(id)||{}).value||''; };
        var mv=v('mainValue');
        return {
          mode:(val('mode')||'usaha'),
          mainValue:mv, mainMeta:v('mainMeta'),
          bagPemodal:v('bagPemodal'), bagPengelola:v('bagPengelola'),
          nisbahOut:v('nisbahOut'), returnPemodal:v('returnPemodal'),
          modal:val('modal'), laba:val('laba'), nisbah:val('nisbah'),
          deposito:val('deposito'), rate:val('rate'), tenor:val('tenor'),
          hasResult:(mv!=='Rp 0' && mv!=='')
        };
      }

      function renderGround(){
        var d=readData(); var isUsaha=(d.mode!=='deposito');
        var ctx='<div class="aic-ctx"><i class="ti ti-arrows-split-2"></i> '+(isUsaha?'Bagi Hasil Usaha (Mudharabah)':'Estimasi Deposito Syariah')+'</div>';
        if(!d.hasResult){ ground.innerHTML=ctx+'<div class="aic-nogauge">Klik "Hitung Sekarang" dulu untuk lihat hasilnya.</div>'; return; }
        var rowf=function(l,val){ return '<div style="display:flex;justify-content:space-between;gap:10px;font-size:12.5px;padding:3px 0;border-bottom:1px solid rgba(120,120,120,.15)"><span>'+l+'</span><b>'+esc(val)+'</b></div>'; };
        var rows='';
        if(isUsaha){
          if(d.nisbahOut && d.nisbahOut!=='0:0' && d.nisbahOut!=='0 : 0') rows+=rowf('Nisbah (pemodal:pengelola)', d.nisbahOut);
          if(d.bagPemodal && d.bagPemodal!=='Rp 0') rows+=rowf('Bagian pemodal', d.bagPemodal);
          if(d.bagPengelola && d.bagPengelola!=='Rp 0') rows+=rowf('Bagian pengelola', d.bagPengelola);
          if(d.returnPemodal && d.returnPemodal!=='\u2014') rows+=rowf('Return pemodal', d.returnPemodal+'/th');
        } else {
          rows+=rowf('Estimasi bagi hasil', d.mainValue);
          if(d.mainMeta) rows+=rowf('Indikasi', d.mainMeta);
        }
        var verdict='<div class="aic-verdict warn"><i class="ti ti-info-circle"></i> '+(isUsaha?'Bagi hasil naik-turun mengikuti laba riil usaha \u2014 tidak dijamin tetap seperti bunga.':'Angka ini indikasi bagi hasil, bukan bunga tetap yang dijamin.')+'</div>';
        ground.innerHTML=ctx+'<div style="margin:8px 0">'+rows+'</div>'+verdict;
      }

      function buildContext(){
        var d=readData(); var isUsaha=(d.mode!=='deposito');
        var p=[
          'Kalkulator: Bagi Hasil Syariah \u2014 mode '+(isUsaha?'bagi hasil usaha (mudharabah/musyarakah)':'estimasi deposito syariah')+'.',
          isUsaha
            ? ('Input: modal/investasi Rp '+d.modal+', laba usaha Rp '+d.laba+', nisbah pemodal '+d.nisbah+'%. Hasil: bagian pemodal '+d.bagPemodal+', bagian pengelola '+d.bagPengelola+', nisbah '+d.nisbahOut+', return pemodal '+d.returnPemodal+'/tahun.')
            : ('Input: dana Rp '+d.deposito+', rate indikasi '+d.rate+'%/tahun, tenor '+d.tenor+' bulan. Hasil: '+d.mainValue+(d.mainMeta?(' ('+d.mainMeta+')'):'')+'.'),
          'Acuan untuk jawabanmu: Bagi hasil syariah memakai akad mudharabah (pemodal/shahibul mal menyerahkan modal, pengelola/mudharib menjalankan usaha) atau musyarakah (kedua pihak sama-sama menyetor modal & berbagi untung-rugi). NISBAH adalah rasio pembagian LABA (mis. 60:40), disepakati di awal, dihitung dari KEUNTUNGAN usaha \u2014 BUKAN dari omzet atau dari modal. Inilah yang membedakannya dari bunga (riba): imbalan bergantung pada hasil usaha yang riil dan bisa naik-turun, tidak dijamin tetap. Bila usaha rugi bukan karena kelalaian pengelola, kerugian modal ditanggung pemodal sedangkan pengelola menanggung rugi tenaga/waktu. Deposito syariah juga memakai akad mudharabah, sehingga "rate" yang tampil adalah indikasi bagi hasil, bukan bunga tetap yang dijamin. Return lebih tinggi umumnya diiringi risiko lebih tinggi. Untuk akad yang mengikat, sarankan konsultasi ke lembaga keuangan syariah atau Dewan Pengawas Syariah. Pakai sapaan "kamu", jawab ringkas & hangat, dan JANGAN mengarang angka di luar data.'
        ];
        return p.filter(Boolean).join('\n');
      }

      function mdInline(t){ t=t.replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>'); t=t.replace(/__([^_]+)__/g,'<strong>$1</strong>'); t=t.replace(/(^|[\s(])\*([^*\n]+)\*(?=[\s).,!?:;]|$)/g,'$1<em>$2</em>'); t=t.replace(/`([^`]+)`/g,'<code>$1</code>'); return t; }
      function mdToHtml(raw){ var lines=String(raw).replace(/\r/g,'').split('\n'); var out=[]; for(var i=0;i<lines.length;i++){ var t=lines[i].trim(); if(t==='')continue; if(/^([-*_]\s*){3,}$/.test(t))continue; var h=t.match(/^#{1,6}\s+(.*)$/); if(h){out.push('<p class="aic-h">'+mdInline(esc(h[1]))+'</p>');continue;} var b=t.match(/^[-*\u2022]\s+(.*)$/); if(b){out.push('<div class="aic-li"><span class="aic-mk">\u2022</span><span>'+mdInline(esc(b[1]))+'</span></div>');continue;} var n=t.match(/^(\d+)[.)]\s+(.*)$/); if(n){out.push('<div class="aic-li"><span class="aic-mk">'+n[1]+'.</span><span>'+mdInline(esc(n[2]))+'</span></div>');continue;} out.push('<p>'+mdInline(esc(t))+'</p>'); } return out.join('')||('<p>'+mdInline(esc(String(raw)))+'</p>'); }
      function typeInto(bubble, text, done){ var n=0, len=text.length, step=Math.max(2, Math.round(len/90)); var t=setInterval(function(){ n=Math.min(len, n+step); bubble.innerHTML=mdToHtml(text.slice(0,n)); msgs.scrollTop=msgs.scrollHeight; if(n>=len){ clearInterval(t); done&&done(); } }, 18); }
      function mkCopyBtn(text){ var b=document.createElement('button'); b.type='button'; b.className='aic-copy'; b.innerHTML='<i class="ti ti-copy"></i><span>Salin</span>'; b.addEventListener('click', function(){ HC.copyText(text, 'Jawaban tersalin!'); }); return b; }
      function addMsgTyped(text){ var row=document.createElement('div'); row.className='aic-row ai'; row.innerHTML='<div class="aic-av"><i class="ti ti-sparkles"></i></div><div class="aic-bub aic-rich"></div>'; var bub=row.querySelector('.aic-bub'); msgs.appendChild(row); msgs.scrollTop=msgs.scrollHeight; typeInto(bub, text, function(){ bub.appendChild(mkCopyBtn(text)); renderFollowups(); }); return row; }
      var FOLLOWUPS=['Nisbah 60:40 maksudnya apa?','Bagi hasil beda sama bunga?','Kalau usaha rugi gimana?','Bagi hasil dari laba atau omzet?','Apa itu mudharabah & musyarakah?','Return ini bagus gak?','Deposito syariah aman?','Cara nentukan nisbah adil?'];
      function renderFollowups(){ chips.innerHTML=''; var asked=history.filter(function(m){return m.role==='user';}).map(function(m){return m.content;}); var pool=FOLLOWUPS.filter(function(q){return asked.indexOf(q)<0;}); pool.slice(0,3).forEach(function(q){ var b=document.createElement('button'); b.type='button'; b.className='aic-chip'; b.textContent=q; b.addEventListener('click', function(){ ask(q); }); chips.appendChild(b); }); }
      function addMsg(role,text){ var row=document.createElement('div'); row.className='aic-row '+role; if(role==='ai'){ row.innerHTML='<div class="aic-av"><i class="ti ti-sparkles"></i></div><div class="aic-bub aic-rich"></div>'; row.querySelector('.aic-bub').innerHTML=mdToHtml(text); } else { row.innerHTML='<div class="aic-bub"></div>'; row.querySelector('.aic-bub').textContent=text; } msgs.appendChild(row); msgs.scrollTop=msgs.scrollHeight; return row; }
      function addTyping(){ var row=document.createElement('div'); row.className='aic-row ai'; row.innerHTML='<div class="aic-av"><i class="ti ti-sparkles"></i></div><div class="aic-bub aic-typing"><span></span><span></span><span></span></div>'; msgs.appendChild(row); msgs.scrollTop=msgs.scrollHeight; return row; }
      function todayKey(){ var d=new Date(); return 'hc-coach-'+d.getFullYear()+'-'+(d.getMonth()+1)+'-'+d.getDate(); }
      function getCount(){ try{ return parseInt(localStorage.getItem(todayKey())||'0',10)||0; }catch(e){ return 0; } }
      function incCount(){ try{ localStorage.setItem(todayKey(), getCount()+1); }catch(e){} }
      function refreshLimit(){ var left=Math.max(0,LIMIT-getCount()); limitEl.textContent='Sisa '+left+' pertanyaan gratis hari ini.'; var done=left<=0; input.disabled=done; send.disabled=done; if(done){ input.placeholder='Batas harian tercapai, balik lagi besok ya.'; } }
      var busy=false;
      async function ask(text){ text=(text||'').trim(); if(!text||busy)return; if(getCount()>=LIMIT){refreshLimit();return;} busy=true; send.disabled=true; addMsg('user',text); history.push({role:'user',content:text}); input.value=''; chips.innerHTML=''; var typing=addTyping(); incCount();
        try{ var res=await fetch('/api/coach',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({calcContext:buildContext(),messages:history})}); var data=await res.json().catch(function(){return {error:'Respon tidak valid.'};}); typing.parentNode&&typing.remove(); if(data&&data.reply){ addMsgTyped(data.reply); history.push({role:'assistant',content:data.reply}); } else { addMsg('ai','\u26a0\ufe0f '+((data&&data.error)||'Coach lagi sibuk, coba lagi sebentar ya.')); } }
        catch(e){ typing.parentNode&&typing.remove(); addMsg('ai','\u26a0\ufe0f Gagal terhubung ke Coach. Coba lagi ya.'); }
        busy=false; refreshLimit(); if(!input.disabled) input.focus(); }
      var PRESETS=['Nisbah 60:40 maksudnya apa?','Bagi hasil beda sama bunga?','Kalau usaha rugi gimana?'];
      function renderChips(){ chips.innerHTML=''; PRESETS.forEach(function(q){ var b=document.createElement('button'); b.type='button'; b.className='aic-chip'; b.textContent=q; b.addEventListener('click',function(){ask(q);}); chips.appendChild(b); }); }
      var opened=false;
      trigger.addEventListener('click', function(){ if(panel.hasAttribute('hidden')){ panel.removeAttribute('hidden'); wrap.classList.add('open'); applyTheme(); renderGround(); refreshLimit(); if(!opened){renderChips();opened=true;} } else { panel.setAttribute('hidden',''); wrap.classList.remove('open'); } });
      send.addEventListener('click', function(){ ask(input.value); });
      input.addEventListener('keydown', function(e){ if(e.key==='Enter'){ e.preventDefault(); ask(input.value); } });
      var bh=$('btnHitung'); if(bh){ bh.addEventListener('click', function(){ setTimeout(function(){ if(!panel.hasAttribute('hidden')) renderGround(); },800); }); }
    });
  })();
</script>
</Layout>

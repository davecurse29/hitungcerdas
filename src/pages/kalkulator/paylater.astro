---
import Layout from '../../layouts/Layout.astro';
import EbookPromo from '../../components/EbookPromo.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';
import FAQ from '../../components/FAQ.astro';
import AdSlot from '../../components/AdSlot.astro';
import CalculatorCard from '../../components/CalculatorCard.astro';
import { getCalculatorBySlug, getRelatedCalculators } from '../../data/calculators';

const calc = getCalculatorBySlug('paylater')!;
const related = getRelatedCalculators('paylater', 3);

const faqs = [
  { question: 'Apa itu paylater (BNPL)?', answer: 'Paylater atau <strong>Buy Now Pay Later (BNPL)</strong> adalah fasilitas beli sekarang bayar nanti — seperti kartu kredit digital. Kamu bisa belanja dan membayarnya belakangan, sekaligus atau dicicil. Di Indonesia, BNPL diatur OJK melalui <strong>POJK 32 Tahun 2025</strong>, dan hanya boleh diselenggarakan bank atau perusahaan pembiayaan berizin.' },
  { question: 'Apakah bunga 0% berarti gratis?', answer: 'Belum tentu. Bunga 0% hanya berarti tidak ada bunga, <strong>tapi biaya admin, biaya layanan, atau materai bisa tetap ada</strong>. Selalu cek total biaya, bukan hanya bunga. Kalkulator ini menghitung admin terpisah agar kamu lihat total sebenarnya.' },
  { question: 'Apa bedanya bunga flat dan efektif/anuitas?', answer: 'Pada <strong>bunga flat</strong>, bunga dihitung dari pokok awal dan tetap tiap bulan — umum di paylater. Pada <strong>efektif/anuitas</strong>, bunga dihitung dari sisa pokok yang menyusut, sehingga total bunga biasanya lebih kecil untuk persentase yang sama. Karena itu, "2% flat" tidak sama dengan "2% efektif".' },
  { question: 'Apa risiko telat bayar paylater?', answer: 'Telat bayar menambah <strong>denda</strong> yang bisa berakumulasi tiap hari, dan riwayatmu dicatat di <strong>SLIK OJK</strong> (dulu BI Checking). Skor kredit yang buruk bisa menyulitkan pengajuan KPR, kredit kendaraan, atau pinjaman lain di masa depan. Sebagai pengaman, total biaya paylater legal dibatasi maksimal 100% dari pokok.' },
  { question: 'Apakah hasil kalkulator ini sama dengan tagihan aplikasi?', answer: 'Tidak selalu. Ini <strong>simulasi edukatif</strong>. Hasil aktual bergantung pada bunga, biaya admin, promo, denda, dan tanggal jatuh tempo masing-masing penyedia. Selalu cek rincian biaya di aplikasi paylater sebelum menyetujui transaksi.' },
];

const bcItems = [{ label: 'Beranda', href: '/' }, { label: 'Kalkulator', href: '/#tools' }, { label: calc.shortTitle }];
---

<Layout title={calc.title} description={calc.metaDescription} keywords={calc.keywords} canonical={`https://hitungcerdas.net/kalkulator/${calc.slug}`}>
  <section class="calc-hero" style="--cat-color:#DB2777;--cat-color2:#9D174D">
    <div class="wrap">
      <Breadcrumb items={bcItems} />
      <div class="calc-hero-grid">
        <div class="calc-hero-text">
          <div class="calc-hero-tag" style="background:#FCE7F3;color:#9D174D"><i class="ti ti-shopping-cart"></i> {calc.categoryLabel}</div>
          <h1 class="calc-hero-title">{calc.h1}</h1>
          <p class="calc-hero-sub">{calc.introText}</p>
          <ul class="calc-hero-benefits">{calc.benefits.map(b => <li><i class="ti ti-check"></i> {b}</li>)}</ul>
        </div>
        <div class="calc-hero-illust">
          <svg viewBox="0 0 200 200" fill="none">
            <circle cx="100" cy="100" r="85" fill="rgba(255,255,255,.08)"/>
            <rect x="55" y="74" width="90" height="62" rx="10" fill="#FFF" stroke="#FBBF24" stroke-width="3"/>
            <rect x="55" y="86" width="90" height="13" fill="#DB2777"/>
            <rect x="64" y="112" width="34" height="7" rx="3.5" fill="#9D174D" opacity=".6"/>
            <rect x="120" y="110" width="17" height="11" rx="2.5" fill="#FBBF24"/>
            <circle cx="100" cy="58" r="13" fill="none" stroke="#FFF" stroke-width="5"/>
            <path d="M91 58h18" stroke="#FBBF24" stroke-width="4" stroke-linecap="round"/>
          </svg>
        </div>
      </div>
    </div>
  </section>

  <section class="calc-section">
    <div class="wrap">
      <div class="calc-card">
        <div class="calc-card-hdr">
          <div class="calc-card-hdr-ic" style="background:linear-gradient(135deg,#DB2777 0%,#9D174D 100%)"><i class="ti ti-shopping-cart"></i></div>
          <div><h2 class="calc-card-title">Kalkulator Paylater (BNPL)</h2><p class="calc-card-sub">Hitung cicilan, total bayar &amp; selisihnya dibanding bayar tunai</p></div>
        </div>

        <div class="calc-grid">
          <div class="calc-input">
            <div class="field-group-label" style="font-weight:700;color:#9D174D;margin:2px 0 6px;font-size:.9rem"><i class="ti ti-shopping-bag"></i> Detail Pembelian</div>

            <div class="field"><label>Harga Barang/Jasa</label>
              <div class="input-wrap"><span class="input-prefix">Rp</span>
                <input type="text" id="harga" class="has-prefix" value="1.000.000" inputmode="numeric" /></div></div>

            <div class="field"><label>DP / Bayar di Awal <span class="hint">(opsional)</span></label>
              <div class="input-wrap"><span class="input-prefix">Rp</span>
                <input type="text" id="dp" class="has-prefix" value="0" inputmode="numeric" /></div></div>

            <div class="field"><label>Diskon / Voucher Langsung <span class="hint">(opsional)</span></label>
              <div class="input-wrap"><span class="input-prefix">Rp</span>
                <input type="text" id="diskon" class="has-prefix" value="0" inputmode="numeric" /></div></div>

            <div class="field-group-label" style="font-weight:700;color:#9D174D;margin:14px 0 6px;font-size:.9rem"><i class="ti ti-calendar-dollar"></i> Skema Cicilan</div>

            <div class="field"><label>Tenor <span class="hint">(lama cicilan)</span></label>
              <select id="tenor">
                <option value="1">1 bulan</option>
                <option value="3">3 bulan</option>
                <option value="6">6 bulan</option>
                <option value="12" selected>12 bulan</option>
                <option value="24">24 bulan</option>
              </select></div>

            <div class="field"><label>Metode Perhitungan Bunga</label>
              <select id="metode">
                <option value="flat" selected>Bunga Flat per Bulan (umum di paylater)</option>
                <option value="anuitas">Bunga Efektif/Anuitas (cicilan tetap)</option>
                <option value="promo0">Promo Bunga 0%</option>
              </select></div>

            <div class="field"><label>Bunga / Margin per Bulan <span class="hint">(%)</span></label>
              <input type="number" id="bunga" value="2.95" min="0" step="0.05" /></div>

            <div style="display:flex;flex-wrap:wrap;gap:7px;margin:2px 0 12px">
              <button type="button" class="bunga-chip" data-bunga="promo0" style="font-size:.8rem;padding:7px 12px;border:1px solid #FBCFE8;background:#FDF2F8;color:#9D174D;border-radius:8px;cursor:pointer;font-weight:600">Promo 0%</button>
              <button type="button" class="bunga-chip" data-bunga="2.95" style="font-size:.8rem;padding:7px 12px;border:1px solid #FBCFE8;background:#FDF2F8;color:#9D174D;border-radius:8px;cursor:pointer;font-weight:600">2,95%/bln</button>
              <button type="button" class="bunga-chip" data-bunga="5" style="font-size:.8rem;padding:7px 12px;border:1px solid #FBCFE8;background:#FDF2F8;color:#9D174D;border-radius:8px;cursor:pointer;font-weight:600">5%/bln</button>
            </div>

            <div class="field"><label>Biaya Admin di Awal <span class="hint">(opsional)</span></label>
              <div class="input-wrap"><span class="input-prefix">Rp</span>
                <input type="text" id="adminRp" class="has-prefix" value="0" inputmode="numeric" /></div></div>

            <div class="adv-section" style="margin:8px 0 14px">
              <div class="adv-toggle" style="cursor:pointer;display:flex;align-items:center;justify-content:space-between;padding:11px 13px;background:#FDF2F8;border:1px solid #FBCFE8;border-radius:10px"><span style="font-size:.88rem;font-weight:600;color:#9D174D"><i class="ti ti-plus"></i>&nbsp; Biaya tambahan (ongkir, proteksi, admin %)</span><i class="ti ti-chevron-down chev"></i></div>
              <div class="adv-content">
                <div class="field"><label>Admin Awal <span class="hint">(% dari nilai dibiayai)</span></label>
                  <input type="number" id="adminPct" value="0" min="0" step="0.1" /></div>
                <div class="field"><label>Ongkir / Pengiriman</label><div class="input-wrap"><span class="input-prefix">Rp</span><input type="text" id="ongkir" class="has-prefix" value="0" inputmode="numeric" /></div></div>
                <div class="field"><label>Biaya Proteksi / Asuransi</label><div class="input-wrap"><span class="input-prefix">Rp</span><input type="text" id="proteksi" class="has-prefix" value="0" inputmode="numeric" /></div></div>
                <div class="field"><label>Biaya Admin per Cicilan</label><div class="input-wrap"><span class="input-prefix">Rp</span><input type="text" id="adminCicilan" class="has-prefix" value="0" inputmode="numeric" /></div></div>
                <div class="field"><label>Biaya Lain (materai, layanan)</label><div class="input-wrap"><span class="input-prefix">Rp</span><input type="text" id="biayaLain" class="has-prefix" value="0" inputmode="numeric" /></div></div>
              </div>
            </div>

            <div class="field-group-label" style="font-weight:700;color:#9D174D;margin:8px 0 6px;font-size:.9rem"><i class="ti ti-wallet"></i> Cek Kemampuan Bayar <span class="hint" style="font-weight:400">(opsional)</span></div>
            <div class="field"><label>Penghasilan Bulanan Bersih</label><div class="input-wrap"><span class="input-prefix">Rp</span><input type="text" id="penghasilan" class="has-prefix" value="0" inputmode="numeric" /></div></div>
            <div class="field"><label>Cicilan Utang Lain per Bulan <span class="hint">(KPR, KTA, paylater lain)</span></label><div class="input-wrap"><span class="input-prefix">Rp</span><input type="text" id="cicilanLain" class="has-prefix" value="0" inputmode="numeric" /></div></div>

            <div class="adv-section" style="margin:0 0 14px">
              <label style="display:flex;align-items:center;gap:10px;padding:11px 13px;background:#FEF2F2;border:1px solid #FECACA;border-radius:10px;cursor:pointer;font-size:.88rem">
                <input type="checkbox" id="pakaiDenda" style="width:18px;height:18px;accent-color:#DC2626;flex-shrink:0" />
                <span style="font-weight:600;color:#B91C1C"><i class="ti ti-alert-triangle"></i> Simulasikan kalau telat bayar (galbay)</span></label>
              <div id="dendaFields" style="display:none;margin-top:10px">
                <div class="field"><label>Telat Berapa Hari?</label><input type="number" id="hariTelat" value="30" min="1" max="365" /></div>
                <div class="field"><label>Metode Denda</label>
                  <select id="metodeDenda">
                    <option value="pctBulan" selected>% per bulan dari cicilan (umum, mis. 5%)</option>
                    <option value="pctHari">% per hari dari cicilan</option>
                    <option value="tetap">Rp tetap per bulan telat</option>
                  </select></div>
                <div class="field" id="fDendaPct"><label>Besaran Denda <span class="hint">(% dari cicilan)</span></label><input type="number" id="dendaPct" value="5" min="0" step="0.05" /></div>
                <div class="field" id="fDendaRp" style="display:none"><label>Denda Tetap <span class="hint">(Rp per bulan telat)</span></label><div class="input-wrap"><span class="input-prefix">Rp</span><input type="text" id="dendaRp" class="has-prefix" value="0" inputmode="numeric" /></div></div>
                <div class="field"><label>Batas Maksimal Denda <span class="hint">(Rp, 0 = tanpa batas)</span></label><div class="input-wrap"><span class="input-prefix">Rp</span><input type="text" id="dendaMax" class="has-prefix" value="0" inputmode="numeric" /></div></div>
              </div>
            </div>

            <div class="calc-action-bar">
              <button type="button" class="btn-hitung" id="btnHitung"><i class="ti ti-calculator"></i> Hitung Cicilan</button>
              <button type="button" class="btn-reset" id="btnReset"><i class="ti ti-refresh"></i> Reset</button>
            </div>
          </div>

          <div class="calc-output">
            <div class="hist-section" id="histBox" style="display:none"></div>

            <div class="result-placeholder" id="placeholder">
              <i class="ti ti-shopping-cart result-placeholder-icon"></i>
              <div class="result-placeholder-title">Simulasi cicilan paylater akan muncul di sini</div>
              <div class="result-placeholder-desc">Isi harga, tenor, dan bunga, lalu klik <strong>"Hitung Cicilan"</strong>.</div>
            </div>

            <div class="result-wrapper" id="results">
              <div class="result-main">
                <div class="result-label">Cicilan per Bulan</div>
                <div class="result-value" id="cicilanBln">Rp 0</div>
                <div class="result-meta"><i class="ti ti-info-circle"></i> <span id="metaInfo">Hasil simulasi</span></div>
              </div>

              <div class="result-grid">
                <div class="result-item"><div class="result-item-label">Nilai Dibiayai</div><div class="result-item-value" id="nilaiDibiayai">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">Total Bunga/Margin</div><div class="result-item-value" id="totalBunga">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">Total Biaya &amp; Admin</div><div class="result-item-value" id="totalBiaya">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">Total Bayar</div><div class="result-item-value result-strong" id="totalBayar">Rp 0</div></div>
              </div>

              <div class="kelayakan-box">
                <div class="kelayakan-hdr"><i class="ti ti-scale"></i><span>Ringkasan &amp; Catatan Jujur</span></div>
                <div id="ringkasanBody"></div>

                <div style="margin-top:14px">
                  <div style="font-size:.82rem;font-weight:700;color:#64748b;margin-bottom:7px">Harga Tunai vs Total Bayar Paylater</div>
                  <div style="display:flex;align-items:center;gap:8px;margin-bottom:6px"><span style="width:52px;font-size:.78rem;color:#64748b">Tunai</span><div style="flex:1;background:#f1f5f9;border-radius:6px;height:20px;overflow:hidden"><div id="barTunai" style="height:100%;width:0;background:#94a3b8;border-radius:6px;transition:width .5s"></div></div><b id="barTunaiV" style="min-width:108px;text-align:right;font-size:.82rem">Rp 0</b></div>
                  <div style="display:flex;align-items:center;gap:8px"><span style="width:52px;font-size:.78rem;color:#64748b">Paylater</span><div style="flex:1;background:#f1f5f9;border-radius:6px;height:20px;overflow:hidden"><div id="barPL" style="height:100%;width:0;background:#DB2777;border-radius:6px;transition:width .5s"></div></div><b id="barPLV" style="min-width:108px;text-align:right;font-size:.82rem;color:#9D174D">Rp 0</b></div>
                </div>

                <p class="ks-note" style="margin-top:12px;font-size:.8rem;color:#64748b"><i class="ti ti-alert-triangle"></i> Ini <strong>simulasi/estimasi edukatif</strong>. Hasil aktual bergantung kebijakan penyedia (bunga, admin, promo, denda). Selalu cek rincian di aplikasi paylater sebelum setuju. Gunakan hanya jika cicilan sesuai kemampuan bayarmu.</p>
              </div>

              <div id="dendaBox" style="display:none;margin-top:14px;background:#FEF2F2;border:1px solid #FECACA;border-radius:14px;padding:14px 16px">
                <div style="display:flex;align-items:center;gap:8px;font-weight:700;color:#B91C1C;font-size:.92rem;margin-bottom:8px"><i class="ti ti-alert-triangle"></i><span>Kalau Telat Bayar (Galbay)</span></div>
                <div id="dendaBody"></div>
              </div>

              <div class="adv-section" style="margin-top:14px">
                <div class="adv-toggle" style="cursor:pointer;display:flex;align-items:center;justify-content:space-between;padding:11px 13px;background:#FDF2F8;border:1px solid #FBCFE8;border-radius:10px"><span style="font-size:.88rem;font-weight:600;color:#9D174D"><i class="ti ti-arrows-left-right"></i>&nbsp; Bandingkan Tenor (1&ndash;24 bln)</span><i class="ti ti-chevron-down chev"></i></div>
                <div class="adv-content"><div id="compareBox" style="padding-top:6px"></div></div>
              </div>
              <div class="adv-section" style="margin-top:10px">
                <div class="adv-toggle" style="cursor:pointer;display:flex;align-items:center;justify-content:space-between;padding:11px 13px;background:#FDF2F8;border:1px solid #FBCFE8;border-radius:10px"><span style="font-size:.88rem;font-weight:600;color:#9D174D"><i class="ti ti-table"></i>&nbsp; Tabel Jadwal Cicilan</span><i class="ti ti-chevron-down chev"></i></div>
                <div class="adv-content"><div id="jadwalBox" style="padding-top:6px"></div></div>
              </div>

              <div class="export-row">
                <button type="button" class="export-btn img" id="exportImg"><i class="ti ti-photo-down"></i><span>Save Image</span></button>
                <button type="button" class="export-btn wa" id="exportWa"><i class="ti ti-brand-whatsapp"></i><span>Share WA</span></button>
                <button type="button" class="export-btn pdf" id="exportPdf"><i class="ti ti-printer"></i><span>Print/PDF</span></button>
                <button type="button" class="export-btn copy" id="exportCopy"><i class="ti ti-copy"></i><span>Copy Teks</span></button>
                <button type="button" class="export-btn" id="exportExcel"><i class="ti ti-file-spreadsheet" style="color:#1D6F42"></i><span style="color:#1D6F42">Excel</span></button>
              </div>
              <!-- ===== AI COACH KEPUTUSAN (mandiri) ===== -->
              <div class="aic-wrap" id="aicWrap">
                <button type="button" class="aic-trigger" id="aicTrigger">
                  <span class="aic-trigger-ic"><i class="ti ti-sparkles"></i></span>
                  <span class="aic-trigger-txt">
                    <strong>Tanya AI Coach</strong>
                    <small>Bingung worth it atau nggak? Tanya di sini</small>
                  </span>
                  <i class="ti ti-chevron-down aic-trigger-arr"></i>
                </button>
                <div class="aic-panel" id="aicPanel" hidden>
                  <div class="aic-head">
                    <span class="aic-head-ic"><i class="ti ti-sparkles"></i></span>
                    <div class="aic-head-tt">
                      <strong>AI Coach Keputusan</strong>
                      <small>Bantu pahami cicilan paylater-mu</small>
                    </div>
                    <span class="aic-badge">BETA</span>
                  </div>
                  <div class="aic-ground" id="aicGround"></div>
                  <div class="aic-msgs" id="aicMsgs"></div>
                  <div class="aic-chips" id="aicChips"></div>
                  <div class="aic-input">
                    <input type="text" id="aicText" placeholder="Tanya soal cicilanmu..." maxlength="600" autocomplete="off" />
                    <button type="button" id="aicSend" aria-label="Kirim"><i class="ti ti-arrow-up"></i></button>
                  </div>
                  <div class="aic-disc">
                    <i class="ti ti-info-circle"></i>
                    <span>Estimasi edukatif berbasis hasil hitunganmu, bukan nasihat keuangan berlisensi.</span>
                  </div>
                  <div class="aic-limit" id="aicLimit"></div>
                </div>
              </div>
              <!-- ===== /AI COACH ===== -->

            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <EbookPromo calc="paylater" headline="Lagi atur keuangan biar nggak kejebak cicilan? Mulai dari sini." />

  <section class="content-section">
    <div class="wrap-text">
      <h2>Apa Itu Paylater (BNPL)?</h2>
      <p>Paylater (<strong>Buy Now Pay Later</strong>) memungkinkanmu membeli barang/jasa sekarang dan membayarnya belakangan — sekaligus atau dicicil. Praktis, tapi di balik "cicilan kecil" sering ada bunga, biaya admin, dan denda yang membuat <strong>total bayar lebih besar dari harga tunai</strong>. Di Indonesia, BNPL diatur OJK lewat <strong>POJK 32 Tahun 2025</strong> dan hanya boleh diselenggarakan bank atau perusahaan pembiayaan berizin. Sebelum checkout, cek dulu beban cicilanmu — dan siapkan <a href="/kalkulator/dana-darurat">dana darurat</a> sebagai bantalan.</p>

      <h2>Cara Kerja Cicilan Paylater</h2>
      <p>Nilai yang dicicil (<strong>nilai dibiayai</strong>) = harga + ongkir + proteksi − diskon − DP. Dari situ, cicilan per bulan dihitung berdasarkan metode bunga (flat, efektif/anuitas, atau promo 0%) ditambah biaya admin. Kalkulator ini menampilkan <strong>cicilan/bulan, total bunga, total biaya, dan total bayar</strong> sekaligus — supaya kamu tahu angka sebenarnya, bukan cuma cicilan bulanannya.</p>

      <h2>Bunga 0% Belum Tentu Gratis</h2>
      <p>Banyak promo "bunga 0%" tetap memungut <strong>biaya admin atau biaya layanan</strong>. Karena itu kalkulator ini memisahkan bunga dari admin, lalu menghitung <strong>selisihnya dibanding bayar tunai (markup %)</strong>. Sebagai pengaman, OJK menetapkan batas <strong>bunga konsumtif 0,1% per hari (sejak 1 Januari 2026)</strong> dan <strong>Lock Cap 100%</strong> — total pembayaran tidak boleh melebihi dua kali lipat pokok.</p>

      <h2>Pakai Paylater dengan Bijak</h2>
      <p>Gunakan paylater untuk kebutuhan, bukan keinginan impulsif, dan pastikan total cicilan masih sesuai kemampuan bayar. Telat bayar menambah denda dan tercatat di <strong>SLIK OJK</strong>. Pastikan penyedia terdaftar &amp; diawasi OJK — bedakan dari <a href="/kalkulator/pinjol-legal">pinjol legal vs ilegal</a>. Kalau cicilan sudah telanjur berat, pelajari opsi <a href="/kalkulator/restrukturisasi-kredit">restrukturisasi kredit</a> dan <a href="/kalkulator/kta">bandingkan dengan KTA</a>.</p>

      <div class="cta-box"><h3>Hitung Cicilan Paylatermu</h3><p>Isi harga, tenor, dan bunga, lalu klik "Hitung Cicilan" untuk melihat total bayar &amp; selisihnya dibanding tunai.</p></div>
    </div>
  </section>

  <AdSlot format="horizontal" />
  <FAQ title="Pertanyaan Seputar Paylater (BNPL)" items={faqs} />

  <section class="section"><div class="wrap">
    <div class="section-hdr"><div class="section-eyebrow">Kalkulator Lainnya</div><h2 class="section-title">Hitung Juga yang Lain</h2></div>
    <div class="related-grid">{related.map(c => <CalculatorCard calc={c} />)}</div>
  </div></section>
  <p class="hc-method-note" style="text-align:center;font-size:.85rem;color:#64748b;margin:1.2rem auto;max-width:680px;line-height:1.6;padding:0 1rem;">
    📅 Diperbarui <strong>Juni 2026</strong> · Simulasi memakai rumus bunga flat, efektif/anuitas &amp; promo 0%. Estimasi edukatif — bukan layanan pembiayaan, bukan ajakan berutang. Hasil aktual bergantung kebijakan penyedia. <strong>Sumber:</strong> OJK (POJK 32/2025 tentang BNPL; SE OJK 19/SEOJK.05/2023 untuk batas biaya), sikapiuangmu.ojk.go.id.
  </p>

  <script is:inline>
    (function() {
      function ready(fn) { if (window.HC && window.html2canvas) fn(); else setTimeout(() => ready(fn), 100); }
      ready(() => {
        const $ = id => document.getElementById(id);
        const els = { harga: $('harga'), dp: $('dp'), diskon: $('diskon'), tenor: $('tenor'), metode: $('metode'), bunga: $('bunga'), adminRp: $('adminRp'), adminPct: $('adminPct'), ongkir: $('ongkir'), proteksi: $('proteksi'), adminCicilan: $('adminCicilan'), biayaLain: $('biayaLain') };
        ['harga','dp','diskon','adminRp','ongkir','proteksi','adminCicilan','biayaLain','penghasilan','cicilanLain'].forEach(id => { const el = $(id); if (el) el.addEventListener('input', HC.fmtInput); });
        document.querySelectorAll('.adv-toggle').forEach(t => t.addEventListener('click', () => t.parentElement.classList.toggle('open')));
        $('pakaiDenda').addEventListener('change', () => { $('dendaFields').style.display = $('pakaiDenda').checked ? '' : 'none'; });
        $('metodeDenda').addEventListener('change', () => { const m = $('metodeDenda').value; $('fDendaPct').style.display = (m === 'tetap') ? 'none' : ''; $('fDendaRp').style.display = (m === 'tetap') ? '' : 'none'; });
        ['dendaRp','dendaMax'].forEach(id => { const el = $(id); if (el) el.addEventListener('input', HC.fmtInput); });

        let lastCalc = null;
        function zonaDTI(ratio) { if (ratio <= 30) return { l: 'aman', c: '#047857', i: 'ti-circle-check' }; if (ratio <= 40) return { l: 'waspada', c: '#B45309', i: 'ti-alert-triangle' }; return { l: 'berat', c: '#DC2626', i: 'ti-alert-circle' }; }

        // cicilan pokok+bunga per bulan (tanpa admin)
        function cicilanPB(P, rBln, n, metode) {
          if (P <= 0 || n <= 0) return 0;
          if (metode === 'promo0') return P / n;
          if (metode === 'flat') return P / n + P * (rBln / 100);
          const i = rBln / 100; // anuitas, bunga bulanan
          if (i === 0) return P / n;
          return P * i / (1 - Math.pow(1 + i, -n));
        }

        function compareTenorHTML(P, rBln, metode, adminAwal, biayaLain, adminCicilan, dp, hargaBersih, nSel) {
          const tenors = [1, 3, 6, 12, 24];
          let rows = '';
          tenors.forEach(t => {
            const cicPB = cicilanPB(P, rBln, t, metode);
            let tb = metode === 'promo0' ? 0 : (metode === 'flat' ? P * (rBln / 100) * t : cicPB * t - P);
            tb = Math.max(0, tb);
            const cb = cicPB + adminCicilan;
            const tot = dp + adminAwal + biayaLain + cb * t;
            const mk = hargaBersih > 0 ? (tot - hargaBersih) / hargaBersih * 100 : 0;
            const sel = t === nSel;
            rows += '<tr style="border-bottom:1px solid #f1f5f9' + (sel ? ';background:#FCE7F3;font-weight:700' : '') + '"><td style="padding:6px 8px">' + t + ' bln' + (sel ? ' \u2190' : '') + '</td><td style="padding:6px 8px;text-align:right">' + HC.fmt(cb) + '</td><td style="padding:6px 8px;text-align:right">' + HC.fmt(tb) + '</td><td style="padding:6px 8px;text-align:right">' + HC.fmt(tot) + '</td><td style="padding:6px 8px;text-align:right">' + mk.toFixed(0) + '%</td></tr>';
          });
          return '<div style="overflow-x:auto"><table style="width:100%;border-collapse:collapse;font-size:.8rem"><thead><tr style="color:#64748b;border-bottom:1px solid #e2e8f0"><th style="padding:6px 8px;text-align:left">Tenor</th><th style="padding:6px 8px;text-align:right">Cicilan/bln</th><th style="padding:6px 8px;text-align:right">Bunga</th><th style="padding:6px 8px;text-align:right">Total Bayar</th><th style="padding:6px 8px;text-align:right">Markup</th></tr></thead><tbody>' + rows + '</tbody></table></div>'
            + '<p class="ks-note" style="margin-top:8px;font-size:.78rem"><i class="ti ti-info-circle"></i> Tenor lebih pendek = cicilan besar tapi <strong>total lebih murah</strong>. Tenor panjang = cicilan ringan tapi total mahal.</p>';
        }

        function jadwalHTML(P, rBln, n, metode, cicPB, adminCicilan) {
          const i = rBln / 100;
          const showAdmin = adminCicilan > 0;
          let rows = '', sisa = P;
          for (let k = 1; k <= n; k++) {
            let bunga, pokok;
            if (metode === 'promo0') { bunga = 0; pokok = P / n; }
            else if (metode === 'flat') { bunga = P * (rBln / 100); pokok = P / n; }
            else { bunga = sisa * i; pokok = cicPB - bunga; }
            sisa = Math.max(0, sisa - pokok);
            const cicilan = pokok + bunga + adminCicilan;
            rows += '<tr style="border-bottom:1px solid #f1f5f9"><td style="padding:5px 7px">' + k + '</td><td style="padding:5px 7px;text-align:right">' + HC.fmt(pokok) + '</td><td style="padding:5px 7px;text-align:right">' + HC.fmt(bunga) + '</td>' + (showAdmin ? '<td style="padding:5px 7px;text-align:right">' + HC.fmt(adminCicilan) + '</td>' : '') + '<td style="padding:5px 7px;text-align:right;font-weight:600">' + HC.fmt(cicilan) + '</td><td style="padding:5px 7px;text-align:right">' + HC.fmt(sisa) + '</td></tr>';
          }
          const head = '<tr style="color:#64748b;border-bottom:1px solid #e2e8f0"><th style="padding:5px 7px;text-align:left">Ke-</th><th style="padding:5px 7px;text-align:right">Pokok</th><th style="padding:5px 7px;text-align:right">Bunga</th>' + (showAdmin ? '<th style="padding:5px 7px;text-align:right">Admin</th>' : '') + '<th style="padding:5px 7px;text-align:right">Cicilan</th><th style="padding:5px 7px;text-align:right">Sisa</th></tr>';
          return '<div style="overflow:auto;max-height:340px"><table style="width:100%;border-collapse:collapse;font-size:.76rem"><thead>' + head + '</thead><tbody>' + rows + '</tbody></table></div>';
        }

        // Solver IRR bulanan (bisection) untuk estimasi APR efektif
        function monthlyIRR(netReceived, pmt, n) {
          if (netReceived <= 0 || pmt <= 0 || n < 1) return null;
          if (pmt * n <= netReceived + 0.5) return 0; // tak ada biaya
          const f = r => { const a = (r === 0) ? n : (1 - Math.pow(1 + r, -n)) / r; return pmt * a - netReceived; };
          let lo = 0, hi = 5; // 0..500%/bln
          if (f(hi) > 0) return 5; // ekstrem
          for (let it = 0; it < 200; it++) {
            const mid = (lo + hi) / 2, fm = f(mid);
            if (Math.abs(fm) < 1e-4) return mid;
            if (fm > 0) lo = mid; else hi = mid;
          }
          return (lo + hi) / 2;
        }
        function aprStr(v) { return v > 9999 ? '>10.000' : v.toFixed(1); }

        function hitung() {
          const harga = HC.parseRp(els.harga.value);
          const dp = HC.parseRp(els.dp.value);
          const diskon = HC.parseRp(els.diskon.value);
          const n = parseInt(els.tenor.value) || 0;
          const metode = els.metode.value;
          let rBln = Math.max(0, parseFloat(els.bunga.value) || 0);
          if (metode === 'promo0') rBln = 0;
          const adminRp = HC.parseRp(els.adminRp.value);
          const adminPct = Math.max(0, parseFloat(els.adminPct.value) || 0);
          const ongkir = HC.parseRp(els.ongkir.value);
          const proteksi = HC.parseRp(els.proteksi.value);
          const adminCicilan = HC.parseRp(els.adminCicilan.value);
          const biayaLain = HC.parseRp(els.biayaLain.value);

          if (harga <= 0) { HC.toast('Masukkan harga barang dulu', 'error'); return; }
          const hargaBersih = harga + ongkir + proteksi - diskon;
          if (hargaBersih <= 0) { HC.toast('Diskon membuat harga jadi 0 atau minus', 'error'); return; }
          if (dp > hargaBersih) { HC.toast('DP tidak boleh melebihi harga setelah diskon', 'error'); return; }
          const P = hargaBersih - dp; // nilai dibiayai
          if (P <= 0) { HC.toast('Tidak ada yang dibiayai (DP menutup seluruh harga)', 'info'); return; }
          if (n < 1) { HC.toast('Pilih tenor cicilan', 'error'); return; }

          const adminAwal = adminRp + (adminPct / 100) * P;
          const cicPB = cicilanPB(P, rBln, n, metode);
          let totalBunga;
          if (metode === 'promo0') totalBunga = 0;
          else if (metode === 'flat') totalBunga = P * (rBln / 100) * n;
          else totalBunga = cicPB * n - P;
          totalBunga = Math.max(0, totalBunga);

          const cicilanBln = cicPB + adminCicilan;
          const totalCicilan = cicilanBln * n;
          const totalBiaya = adminAwal + biayaLain + adminCicilan * n; // semua biaya admin/lain
          const totalBayar = dp + adminAwal + biayaLain + totalCicilan;
          const totalPembiayaan = totalBayar - dp; // yang dibayar atas pokok dibiayai
          const selisih = totalBayar - hargaBersih; // markup nominal vs tunai
          const markupPct = hargaBersih > 0 ? selisih / hargaBersih * 100 : 0;
          const bungaHarian = rBln / 30; // %/hari setara
          const lockCapExceeded = totalPembiayaan > 2 * P + 1;

          // V2.1: simulasi denda telat (galbay)
          const pakaiDenda = $('pakaiDenda').checked;
          let dendaTotal = 0, hariTelat = 0, bulanTelat = 0, totalDgnDenda = totalBayar;
          if (pakaiDenda) {
            hariTelat = Math.max(0, parseInt($('hariTelat').value) || 0);
            const md = $('metodeDenda').value;
            const dPct = Math.max(0, parseFloat($('dendaPct').value) || 0);
            const dRp = HC.parseRp($('dendaRp').value);
            const dMax = HC.parseRp($('dendaMax').value);
            bulanTelat = Math.max(1, Math.ceil(hariTelat / 30));
            if (md === 'pctBulan') dendaTotal = cicilanBln * (dPct / 100) * bulanTelat;
            else if (md === 'pctHari') dendaTotal = cicilanBln * (dPct / 100) * hariTelat;
            else dendaTotal = dRp * bulanTelat;
            if (dMax > 0) dendaTotal = Math.min(dendaTotal, dMax);
            dendaTotal = Math.max(0, Math.round(dendaTotal));
            totalDgnDenda = totalBayar + dendaTotal;
          }

          const income = HC.parseRp($('penghasilan').value);
          const cicilanLain = HC.parseRp($('cicilanLain').value);
          const dsrBefore = income > 0 ? cicilanLain / income * 100 : 0;
          const dsrAfter = income > 0 ? (cicilanLain + cicilanBln) / income * 100 : 0;

          const monthlyR = monthlyIRR(P - adminAwal - biayaLain, cicilanBln, n);
          let aprPct = (monthlyR !== null && isFinite(monthlyR)) ? (Math.pow(1 + monthlyR, 12) - 1) * 100 : null;
          if (aprPct !== null) aprPct = Math.max(0, aprPct);

          if (!isFinite(cicilanBln) || cicilanBln <= 0) { HC.toast('Angka tidak valid, periksa input', 'error'); return; }

          const metodeTxt = metode === 'flat' ? 'Flat' : (metode === 'anuitas' ? 'Efektif/Anuitas' : 'Promo 0%');

          $('cicilanBln').textContent = HC.fmt(cicilanBln);
          $('metaInfo').innerHTML = n + '\u00d7 cicilan \u00b7 Total bayar <b>' + HC.fmt(totalBayar) + '</b>';
          $('nilaiDibiayai').textContent = HC.fmt(P);
          $('totalBunga').textContent = HC.fmt(totalBunga);
          $('totalBiaya').textContent = HC.fmt(totalBiaya);
          $('totalBayar').textContent = HC.fmt(totalBayar);

          const maxV = Math.max(hargaBersih, totalBayar) || 1;
          $('barTunai').style.width = (hargaBersih / maxV * 100) + '%'; $('barTunaiV').textContent = HC.fmt(hargaBersih);
          $('barPL').style.width = (totalBayar / maxV * 100) + '%'; $('barPLV').textContent = HC.fmt(totalBayar);

          let r = '';
          if (selisih <= 1) {
            r += '<div class="kelayakan-status ks-good"><i class="ti ti-circle-check"></i> Tanpa biaya tambahan</div>'
              + '<p class="ks-note">Total bayarmu <strong>sama dengan harga tunai</strong> (' + HC.fmt(hargaBersih) + '). Promo 0% murni tanpa admin \u2014 aman secara biaya. Tetap pastikan kamu mampu bayar tiap cicilannya.</p>';
          } else {
            r += '<div class="kelayakan-status ks-warn"><i class="ti ti-receipt-2"></i> Cicilan kecil \u2260 lebih murah</div>'
              + '<p class="ks-note">Pakai paylater ini, total bayarmu <strong>' + HC.fmt(totalBayar) + '</strong> \u2014 yaitu <strong>' + HC.fmt(selisih) + ' (markup ' + markupPct.toFixed(1) + '%)</strong> lebih mahal dari harga tunai <strong>' + HC.fmt(hargaBersih) + '</strong>. Cicilan ' + HC.fmt(cicilanBln) + '/bln terlihat ringan, tapi totalnya lebih besar.</p>';
          }
          if (aprPct !== null) {
            r += '<div style="margin-top:10px;padding:10px 12px;background:#FDF2F8;border:1px solid #FBCFE8;border-radius:10px"><div style="font-size:.82rem;color:#9D174D;font-weight:700"><i class="ti ti-percentage"></i> APR efektif (estimasi): <span style="font-size:1.05rem">~' + aprStr(aprPct) + '%/tahun</span></div>'
              + '<div style="font-size:.78rem;color:#64748b;margin-top:3px">Tarif tahunan sebenarnya dari arus kas cicilanmu' + (metode === 'flat' && rBln > 0 ? ' \u2014 jauh di atas "flat ' + rBln + '%/bln" yang tertera' : ((metode === 'promo0' && (adminAwal + biayaLain) > 0) ? ' \u2014 biaya admin membuat "0%" tidak benar-benar 0' : '')) + '. Makin pendek tenor &amp; makin besar biaya, makin tinggi APR-nya.</div></div>';
          }
          if (metode === 'promo0' && (adminAwal + biayaLain + adminCicilan * n) > 0) {
            r += '<p class="ks-note" style="margin-top:8px"><i class="ti ti-info-circle"></i> <strong>Bunga 0% bukan berarti gratis</strong> \u2014 masih ada biaya admin/lain ' + HC.fmt(adminAwal + biayaLain + adminCicilan * n) + '.</p>';
          }
          // Pengaman OJK
          if (lockCapExceeded) {
            r += '<p class="ks-note" style="margin-top:8px;color:#DC2626"><i class="ti ti-alert-circle"></i> <strong>Melebihi Lock Cap 100%:</strong> total bayar atas pokok melampaui <strong>2\u00d7 nilai dibiayai</strong>. Untuk paylater legal, ini tidak diperbolehkan (POJK 32/2025) \u2014 cek ulang biaya/tenor &amp; legalitas penyedia.</p>';
          } else if (P > 0) {
            r += '<p class="ks-note" style="margin-top:8px;color:#047857"><i class="ti ti-shield-check"></i> Total bayar masih di bawah <strong>2\u00d7 pokok</strong> (sesuai batas Lock Cap 100% OJK).</p>';
          }
          if (bungaHarian > 0.1001) {
            r += '<p class="ks-note" style="margin-top:8px;color:#DC2626"><i class="ti ti-alert-triangle"></i> Bunga setara <strong>' + bungaHarian.toFixed(3) + '%/hari</strong> \u2014 <strong>melebihi batas 0,1%/hari</strong> (OJK 2026). Pastikan penyedia legal &amp; cek rincian biayanya.</p>';
          } else if (bungaHarian > 0) {
            r += '<p class="ks-note" style="margin-top:8px"><i class="ti ti-circle-check" style="color:#047857"></i> Bunga setara <strong>\u2248' + bungaHarian.toFixed(3) + '%/hari</strong> \u2014 di bawah batas 0,1%/hari (OJK 2026).</p>';
          }
          if (income > 0) {
            const z = zonaDTI(dsrAfter);
            r += '<p class="ks-note" style="margin-top:8px"><i class="ti ' + z.i + '" style="color:' + z.c + '"></i> <strong>Rasio cicilan (DSR):</strong> ' + dsrBefore.toFixed(0) + '% \u2192 <strong style="color:' + z.c + '">' + dsrAfter.toFixed(0) + '%</strong> dari penghasilan (zona ' + z.l + '). Cicilan paylater ini ' + (cicilanBln / income * 100).toFixed(0) + '% penghasilanmu \u2014 idealnya total cicilan \u226430% (patokan OJK).</p>';
          }
          r += '<p class="ks-note" style="margin-top:8px"><strong>Rincian:</strong> Nilai dibiayai ' + HC.fmt(P) + ' \u00b7 Bunga ' + HC.fmt(totalBunga) + ' \u00b7 Biaya ' + HC.fmt(totalBiaya) + ' \u00b7 Metode ' + metodeTxt + (metode !== 'promo0' ? ' ' + rBln + '%/bln' : '') + '.</p>';
          $('ringkasanBody').innerHTML = r;

          if (pakaiDenda && hariTelat > 0) {
            let d = '<p class="ks-note">Telat <strong>' + hariTelat + ' hari</strong> (' + bulanTelat + ' bln telat) \u2192 estimasi denda <strong style="color:#DC2626">' + HC.fmt(dendaTotal) + '</strong>.</p>';
            d += '<p class="ks-note" style="margin-top:6px">Total bayar membengkak: ' + HC.fmt(totalBayar) + ' \u2192 <strong style="color:#DC2626">' + HC.fmt(totalDgnDenda) + '</strong>.</p>';
            if ((totalDgnDenda - dp) > 2 * P + 1) {
              d += '<p class="ks-note" style="margin-top:6px;color:#B91C1C"><i class="ti ti-alert-circle"></i> Dengan denda, total melampaui <strong>2\u00d7 pokok</strong> (batas Lock Cap 100% OJK). Penyedia legal tidak boleh menagih melebihi ini.</p>';
            }
            d += '<p class="ks-note" style="margin-top:6px"><i class="ti ti-database"></i> <strong>Riwayat telat dicatat di SLIK OJK</strong> (dulu BI Checking) \u2014 bisa menyulitkan pengajuan KPR, kredit kendaraan, atau pinjaman lain ke depan.</p>';
            d += '<p class="ks-note" style="margin-top:6px"><i class="ti ti-bulb"></i> Jangan dihindari kalau telanjur berat \u2014 hubungi penyedia untuk <a href="/kalkulator/restrukturisasi-kredit">restrukturisasi</a> &amp; cek <a href="/kalkulator/pinjol-legal">legalitasnya</a>.</p>';
            $('dendaBody').innerHTML = d;
            $('dendaBox').style.display = '';
          } else {
            $('dendaBox').style.display = 'none';
          }

          $('compareBox').innerHTML = compareTenorHTML(P, rBln, metode, adminAwal, biayaLain, adminCicilan, dp, hargaBersih, n);
          $('jadwalBox').innerHTML = jadwalHTML(P, rBln, n, metode, cicPB, adminCicilan);

          lastCalc = { harga, dp, diskon, hargaBersih, P, n, metode, metodeTxt, rBln, adminAwal, biayaLain, adminCicilan, ongkir, proteksi, cicilanBln, totalBunga, totalBiaya, totalCicilan, totalBayar, selisih, markupPct, bungaHarian, pakaiDenda, hariTelat, bulanTelat, dendaTotal, totalDgnDenda, income, cicilanLain, dsrBefore, dsrAfter, aprPct };
          if (HC.history) HC.history.push('paylater', {
            inputs: HC.history.capture(['harga','dp','diskon','tenor','metode','bunga','adminRp','adminPct','ongkir','proteksi','adminCicilan','biayaLain','pakaiDenda','hariTelat','metodeDenda','dendaPct','dendaRp','dendaMax','penghasilan','cicilanLain']),
            summary: HC.fmt(hargaBersih) + ' \u00b7 ' + n + 'bln \u00b7 ' + metodeTxt + (metode !== 'promo0' ? ' ' + rBln + '%' : ''),
            metrics: [{ k: 'Cicilan/bln', v: HC.fmt(cicilanBln) }, { k: 'Total bayar', v: HC.fmt(totalBayar) }]
          });
          renderHist();
          $('placeholder').style.display = 'none';
          $('results').classList.add('show');
          $('results').scrollIntoView({ behavior: 'smooth', block: 'nearest' });
          HC.toast('Simulasi cicilan selesai!');
        }

        function reset() {
          els.harga.value = '1.000.000'; els.dp.value = '0'; els.diskon.value = '0';
          els.tenor.value = '12'; els.metode.value = 'flat'; els.bunga.value = '2.95';
          els.adminRp.value = '0'; els.adminPct.value = '0'; els.ongkir.value = '0';
          els.proteksi.value = '0'; els.adminCicilan.value = '0'; els.biayaLain.value = '0';
          $('penghasilan').value = '0'; $('cicilanLain').value = '0';
          $('placeholder').style.display = ''; $('results').classList.remove('show');
          $('pakaiDenda').checked = false; $('dendaFields').style.display = 'none'; $('dendaBox').style.display = 'none';
          $('hariTelat').value = '30'; $('metodeDenda').value = 'pctBulan'; $('dendaPct').value = '5'; $('dendaRp').value = '0'; $('dendaMax').value = '0';
          $('fDendaPct').style.display = ''; $('fDendaRp').style.display = 'none';
          lastCalc = null; HC.toast('Form direset', 'info');
        }

        function bungaPreset(val) {
          if (val === 'promo0') { els.metode.value = 'promo0'; els.bunga.value = '0'; }
          else { if (els.metode.value === 'promo0') els.metode.value = 'flat'; els.bunga.value = val; }
          if (lastCalc) hitung();
        }

        function buildReport() {
          if (!lastCalc) return null;
          const c = lastCalc;
          const insight = c.selisih <= 1
            ? 'Promo 0% tanpa biaya: total = harga tunai ' + HC.fmt(c.hargaBersih) + '. Pastikan tetap mampu bayar tiap cicilan.'
            : 'Total bayar ' + HC.fmt(c.totalBayar) + ' \u2014 ' + HC.fmt(c.selisih) + ' (markup ' + c.markupPct.toFixed(1) + '%) lebih mahal dari tunai. Cicilan kecil belum tentu lebih murah.';
          const inputs = [
            { label: '\ud83d\udecd\ufe0f Harga Barang', value: HC.fmt(c.harga) },
            { label: '\u23f3 Tenor', value: c.n + ' bulan' },
            { label: '\ud83d\udcc9 Metode', value: c.metodeTxt + (c.metode !== 'promo0' ? ' ' + c.rBln + '%/bln' : '') },
          ];
          if (c.dp > 0) inputs.push({ label: '\ud83d\udcb5 DP', value: HC.fmt(c.dp) });
          if (c.diskon > 0) inputs.push({ label: '\ud83c\udff7\ufe0f Diskon', value: HC.fmt(c.diskon) });
          const results = [
            { label: 'Nilai Dibiayai', value: HC.fmt(c.P) },
            { label: 'Cicilan / Bulan', value: HC.fmt(c.cicilanBln), highlight: true },
            { label: 'Total Bunga/Margin', value: HC.fmt(c.totalBunga) },
            { label: 'Total Biaya & Admin', value: HC.fmt(c.totalBiaya) },
            { label: 'Total Bayar', value: HC.fmt(c.totalBayar), highlight: true },
            { label: 'Selisih vs Tunai', value: HC.fmt(c.selisih) + ' (' + c.markupPct.toFixed(1) + '%)' },
          ];
          if (c.aprPct !== null && c.aprPct !== undefined) results.push({ label: 'APR Efektif (est.)', value: '~' + aprStr(c.aprPct) + '%/th' });
          if (c.income > 0) results.push({ label: 'Rasio Cicilan (DSR)', value: c.dsrBefore.toFixed(0) + '% \u2192 ' + c.dsrAfter.toFixed(0) + '%' });
          if (c.pakaiDenda && c.dendaTotal > 0) {
            results.push({ label: 'Estimasi Denda (telat ' + c.hariTelat + 'h)', value: HC.fmt(c.dendaTotal) });
            results.push({ label: 'Total + Denda', value: HC.fmt(c.totalDgnDenda), highlight: true });
          }
          return HC.buildReportCard({
            title: '\ud83d\udecd\ufe0f Simulasi Paylater (BNPL)',
            color: '#DB2777', color2: '#9D174D',
            heroLabel: 'Cicilan / Bulan',
            heroValue: HC.fmt(c.cicilanBln),
            heroSubtext: c.n + '\u00d7 \u00b7 Total bayar ' + HC.fmt(c.totalBayar),
            inputs, results, insight, slug: 'paylater',
          });
        }

        function summaryText() {
          if (!lastCalc) return '';
          const c = lastCalc;
          let s = '\ud83d\udecd\ufe0f *Simulasi Paylater (BNPL) - HitungCerdas*\n\n'
            + 'Harga: ' + HC.fmt(c.harga) + '\n'
            + 'Tenor: ' + c.n + ' bulan\n'
            + 'Metode: ' + c.metodeTxt + (c.metode !== 'promo0' ? ' ' + c.rBln + '%/bln' : '') + '\n'
            + 'Nilai dibiayai: ' + HC.fmt(c.P) + '\n\n'
            + '\ud83d\udcb5 *Cicilan: ' + HC.fmt(c.cicilanBln) + '/bln*\n'
            + 'Total bunga: ' + HC.fmt(c.totalBunga) + '\n'
            + 'Total biaya & admin: ' + HC.fmt(c.totalBiaya) + '\n'
            + '*Total bayar: ' + HC.fmt(c.totalBayar) + '*\n'
            + 'Selisih vs tunai: ' + HC.fmt(c.selisih) + ' (' + c.markupPct.toFixed(1) + '%)\n'
            + (c.aprPct !== null && c.aprPct !== undefined ? 'APR efektif (est.): ~' + aprStr(c.aprPct) + '%/th\n' : '')
            + (c.income > 0 ? 'Rasio cicilan (DSR): ' + c.dsrBefore.toFixed(0) + '% \u2192 ' + c.dsrAfter.toFixed(0) + '%\n' : '')
            + (c.pakaiDenda && c.dendaTotal > 0 ? '\n\u26a0\ufe0f Telat ' + c.hariTelat + ' hari: +denda ' + HC.fmt(c.dendaTotal) + ' \u2192 total ' + HC.fmt(c.totalDgnDenda) + '\n' : '')
            + '\n_Simulasi edukatif, bukan ajakan berutang. Cek rincian di aplikasi penyedia._\n'
            + 'Hitung: https://hitungcerdas.net/kalkulator/paylater';
          return s;
        }

        function renderHist() {
          if (HC.history) HC.history.render('paylater', $('histBox'), function () { hitung(); });
        }

        $('btnHitung').addEventListener('click', hitung);
        $('btnReset').addEventListener('click', reset);
        document.querySelectorAll('.bunga-chip').forEach(btn => btn.addEventListener('click', () => bungaPreset(btn.getAttribute('data-bunga'))));
        $('exportImg').addEventListener('click', () => { const c = buildReport(); if (!c) { HC.toast('Hitung dulu', 'error'); return; } HC.exportToImage(c, 'paylater-' + Date.now()); });
        $('exportWa').addEventListener('click', () => { const c = buildReport(); if (!c) { HC.toast('Hitung dulu', 'error'); return; } HC.shareWhatsApp(c, summaryText(), 'paylater'); });
        $('exportPdf').addEventListener('click', () => { if (!lastCalc) { HC.toast('Hitung dulu', 'error'); return; } HC.exportToPDF(); });
        $('exportCopy').addEventListener('click', () => { if (!lastCalc) { HC.toast('Hitung dulu', 'error'); return; } HC.copyText(summaryText(), 'Tersalin!'); });
        $('exportExcel').addEventListener('click', () => { const c = buildReport(); if (!c) { HC.toast('Hitung dulu', 'error'); return; } HC.exportToExcel(c, 'paylater-' + Date.now()); });

        renderHist();
      });
    })();
  </script>
<style is:global>
  .aic-wrap{margin-top:18px}
  .aic-trigger{width:100%;display:flex;align-items:center;gap:12px;background:#FDF2F8;border:1px solid #FBCFE8;border-radius:14px;padding:13px 15px;cursor:pointer;text-align:left;transition:background .15s;font-family:inherit}
  .aic-trigger:hover{background:#FCE7F3}
  .aic-trigger-ic{width:36px;height:36px;flex-shrink:0;border-radius:50%;background:#DB2777;color:#fff;display:flex;align-items:center;justify-content:center;font-size:19px}
  .aic-trigger-txt{flex:1;display:flex;flex-direction:column;line-height:1.25}
  .aic-trigger-txt strong{font-size:14px;color:#0A2540}
  .aic-trigger-txt small{font-size:12px;color:#64748B}
  .aic-trigger-arr{font-size:18px;color:#64748B;transition:transform .2s}
  .aic-wrap.open .aic-trigger-arr{transform:rotate(180deg)}

  .aic-panel{margin-top:10px;background:#fff;border:1px solid #E5EAF1;border-radius:16px;overflow:hidden;animation:aicIn .2s ease}
  @keyframes aicIn{from{opacity:0;transform:translateY(-6px)}to{opacity:1;transform:none}}
  .aic-head{display:flex;align-items:center;gap:10px;padding:12px 14px;border-bottom:1px solid #E5EAF1}
  .aic-head-ic{width:30px;height:30px;border-radius:50%;background:#FDF2F8;color:#DB2777;display:flex;align-items:center;justify-content:center;font-size:16px}
  .aic-head-tt{flex:1;display:flex;flex-direction:column;line-height:1.2}
  .aic-head-tt strong{font-size:14px;color:#0A2540}
  .aic-head-tt small{font-size:11px;color:#64748B}
  .aic-badge{font-size:10px;font-weight:700;background:#FDF2F8;color:#DB2777;padding:2px 8px;border-radius:8px}

  .aic-ground{padding:12px 14px;border-bottom:1px solid #E5EAF1;background:#F8FAFC}
  .aic-ctx{font-size:12px;color:#475569;margin-bottom:10px}
  .aic-ctx b{color:#0A2540;font-weight:700}
  .aic-gauge{position:relative;padding-top:19px}
  .aic-gauge-mark{position:absolute;top:0;z-index:3}
  .aic-gauge-mark span{display:block;font-size:10px;font-weight:700;background:#0A2540;color:#fff;padding:1px 7px;border-radius:9px;white-space:nowrap;transform:translateX(-50%)}
  .aic-gauge-mark::after{content:"";position:absolute;left:0;top:15px;width:2px;height:15px;background:#0A2540}
  .aic-gauge-bar{display:flex;height:10px;border-radius:5px;overflow:hidden}
  .aic-gauge-bar .z-green{flex:0 0 60%;background:#D1FAE5}
  .aic-gauge-bar .z-amber{flex:0 0 20%;background:#FEF3C7}
  .aic-gauge-bar .z-red{flex:0 0 20%;background:#FEE2E2}
  .aic-gauge-lbl{display:flex;font-size:10px;margin-top:5px}
  .aic-gauge-lbl .g{flex:0 0 60%;color:#059669}
  .aic-gauge-lbl .a{flex:0 0 20%;text-align:center;color:#D97706;font-weight:600}
  .aic-gauge-lbl .r{flex:0 0 20%;text-align:right;color:#DC2626}
  .aic-verdict{font-size:12.5px;font-weight:600;margin-top:11px;display:flex;align-items:center;gap:6px}
  .aic-verdict.good{color:#059669}
  .aic-verdict.warn{color:#D97706}
  .aic-verdict.bad{color:#DC2626}
  .aic-nogauge{font-size:11px;color:#94A3B8;padding:4px 0}

  .aic-msgs{padding:12px 14px;display:flex;flex-direction:column;gap:10px;max-height:320px;overflow-y:auto}
  .aic-msgs:empty{display:none}
  .aic-row{display:flex;gap:8px;align-items:flex-start}
  .aic-row.user{justify-content:flex-end}
  .aic-av{width:24px;height:24px;flex-shrink:0;border-radius:50%;background:#FDF2F8;color:#DB2777;display:flex;align-items:center;justify-content:center;font-size:13px}
  .aic-bub{max-width:82%;font-size:13px;line-height:1.55;border-radius:13px;padding:9px 12px;white-space:pre-wrap;word-wrap:break-word;overflow-wrap:anywhere}
  .aic-row.ai .aic-bub{background:#F1F5F9;color:#0A2540}
  .aic-row.user .aic-bub{background:#DB2777;color:#fff}
  .aic-bub.aic-rich{white-space:normal}
  .aic-bub.aic-rich p{margin:0 0 7px}
  .aic-bub.aic-rich p:last-child{margin-bottom:0}
  .aic-bub.aic-rich p.aic-h{font-weight:700;margin:10px 0 5px}
  .aic-bub.aic-rich p.aic-h:first-child{margin-top:0}
  .aic-bub.aic-rich strong{font-weight:700}
  .aic-bub.aic-rich em{font-style:italic}
  .aic-bub.aic-rich code{background:rgba(0,0,0,.06);padding:1px 5px;border-radius:4px;font-size:12px}
  .aic-bub.aic-rich .aic-li{display:flex;gap:7px;margin:4px 0;align-items:flex-start}
  .aic-bub.aic-rich .aic-li:first-child{margin-top:0}
  .aic-bub.aic-rich .aic-mk{flex-shrink:0;color:#DB2777;font-weight:700;min-width:15px}
  .aic-typing span{display:inline-block;width:6px;height:6px;border-radius:50%;background:#94A3B8;margin:0 1px;animation:aicblink 1.2s infinite}
  .aic-typing span:nth-child(2){animation-delay:.2s}
  .aic-typing span:nth-child(3){animation-delay:.4s}
  @keyframes aicblink{0%,60%,100%{opacity:.3}30%{opacity:1}}
  @keyframes aicIn{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
  .aic-row{animation:aicIn .25s ease both}
  .aic-copy{margin-top:8px;display:inline-flex;align-items:center;gap:5px;font-size:11px;color:#64748B;background:#fff;border:1px solid #E2E8F0;border-radius:7px;padding:3px 9px;cursor:pointer;transition:all .15s}
  .aic-copy:hover{color:#DB2777;border-color:#DB2777;background:#FDF2F8}
  .aic-copy i{font-size:13px}

  .aic-chips{display:flex;flex-wrap:wrap;gap:6px;padding:0 14px 12px}
  .aic-chips:empty{display:none}
  .aic-chip{font-size:11px;background:#FDF2F8;color:#DB2777;border:1px solid #FBCFE8;border-radius:14px;padding:6px 11px;cursor:pointer;font-family:inherit}
  .aic-chip:hover{background:#FCE7F3}

  .aic-input{display:flex;align-items:center;gap:8px;padding:10px 12px;border-top:1px solid #E5EAF1}
  .aic-input input{flex:1;border:1px solid #E5EAF1;border-radius:18px;padding:9px 14px;font-size:13px;background:#F8FAFC;color:#0A2540;outline:none;font-family:inherit}
  .aic-input input:focus{border-color:#DB2777}
  .aic-input button{width:34px;height:34px;flex-shrink:0;border:0;border-radius:50%;background:#DB2777;color:#fff;cursor:pointer;display:flex;align-items:center;justify-content:center;font-size:17px}
  .aic-input button:disabled{background:#CBD5E1;cursor:not-allowed}

  .aic-disc{display:flex;gap:6px;align-items:flex-start;padding:8px 14px;font-size:10.5px;color:#94A3B8;line-height:1.45;border-top:1px solid #F1F5F9}
  .aic-disc i{margin-top:1px}
  .aic-limit{padding:0 14px 10px;font-size:11px;color:#64748B}

  /* ===== DARK MODE (mandiri, via .aic-dark) ===== */
  .aic-dark .aic-trigger{background:#16263B;border-color:#2A445F}
  .aic-dark .aic-trigger:hover{background:#1B2E47}
  .aic-dark .aic-trigger-txt strong{color:#E8EEF6}
  .aic-dark .aic-trigger-txt small{color:#93A4B8}
  .aic-dark .aic-trigger-arr{color:#93A4B8}
  .aic-dark .aic-panel{background:#0E1A2B;border-color:#243A56}
  .aic-dark .aic-head{border-color:#243A56}
  .aic-dark .aic-head-ic{background:#172A44;color:#F9A8D4}
  .aic-dark .aic-head-tt strong{color:#E8EEF6}
  .aic-dark .aic-head-tt small{color:#93A4B8}
  .aic-dark .aic-badge{background:#172A44;color:#F9A8D4}
  .aic-dark .aic-ground{background:#122036;border-color:#243A56}
  .aic-dark .aic-ctx{color:#A9B7C9}
  .aic-dark .aic-ctx b{color:#E8EEF6}
  .aic-dark .aic-gauge-mark span{background:#E8EEF6;color:#0E1A2B}
  .aic-dark .aic-gauge-mark::after{background:#E8EEF6}
  .aic-dark .aic-gauge-bar .z-green{background:rgba(16,185,129,.24)}
  .aic-dark .aic-gauge-bar .z-amber{background:rgba(245,158,11,.24)}
  .aic-dark .aic-gauge-bar .z-red{background:rgba(239,68,68,.24)}
  .aic-dark .aic-gauge-lbl .g{color:#34D399}
  .aic-dark .aic-gauge-lbl .a{color:#FBBF24}
  .aic-dark .aic-gauge-lbl .r{color:#F87171}
  .aic-dark .aic-verdict.good{color:#34D399}
  .aic-dark .aic-verdict.warn{color:#FBBF24}
  .aic-dark .aic-verdict.bad{color:#F87171}
  .aic-dark .aic-av{background:#172A44;color:#F9A8D4}
  .aic-dark .aic-row.ai .aic-bub{background:#1B2C44;color:#E8EEF6}
  .aic-dark .aic-bub.aic-rich .aic-mk{color:#F9A8D4}
  .aic-dark .aic-bub.aic-rich code{background:rgba(255,255,255,.1)}
  .aic-dark .aic-chip{background:rgba(219,39,119,.16);color:#F9A8D4;border-color:#2A445F}
  .aic-dark .aic-chip:hover{background:rgba(219,39,119,.26)}
  .aic-dark .aic-input{border-color:#243A56}
  .aic-dark .aic-input input{background:#16263B;border-color:#2A445F;color:#E8EEF6}
  .aic-dark .aic-input input:focus{border-color:#EC4899}
  .aic-dark .aic-disc{color:#7E8DA1;border-color:#1B2C44}
  .aic-dark .aic-limit{color:#93A4B8}
</style>
<script is:inline>
  (function(){
    var LIMIT = 10;
    function ready(fn){ if(window.HC){ fn(); } else { setTimeout(function(){ready(fn);},120); } }
    function $(id){ return document.getElementById(id); }
    ready(function(){
      var wrap=$('aicWrap'); if(!wrap) return;
      var trigger=$('aicTrigger'), panel=$('aicPanel'), ground=$('aicGround'), msgs=$('aicMsgs'), chips=$('aicChips');
      var input=$('aicText'), send=$('aicSend'), limitEl=$('aicLimit'); var history=[];
      function isDark(){ var de=document.documentElement; return de.classList.contains('dark')||de.getAttribute('data-theme')==='dark'||(function(){try{return localStorage.getItem('hc-theme')==='dark';}catch(e){return false;}})(); }
      function applyTheme(){ wrap.classList.toggle('aic-dark', isDark()); }
      applyTheme();
      try{ new MutationObserver(applyTheme).observe(document.documentElement,{attributes:true,attributeFilter:['class','data-theme']}); }catch(e){}
      window.addEventListener('storage', function(e){ if(e.key==='hc-theme') applyTheme(); });
      function num(v){ try{ return HC.parseRp(String(v||'')); }catch(e){ return 0; } }
      function selText(id){ var x=$(id); return (x&&x.options&&x.selectedIndex>=0)?x.options[x.selectedIndex].text:''; }
      function esc(t){ return String(t).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }

      function readData(){
        var cicTxt=($('cicilanBln')||{}).textContent||'Rp 0';
        var cicBln=num(cicTxt);
        var income=num(($('penghasilan')||{}).value);
        var cicLain=num(($('cicilanLain')||{}).value);
        var ratio=income>0?(cicLain+cicBln)/income*100:null;
        var apr='';
        var rt=(($('ringkasanBody')||{}).innerText)||'';
        var am=rt.match(/APR efektif[^\n]*?(~[^\n]+?\/tahun)/i);
        if(am) apr=am[1].replace(/^~\s*/,'');
        return { cicilanTxt:cicTxt, cicBln:cicBln, ratio:ratio, income:income, cicLain:cicLain, apr:apr,
          harga:($('harga')||{}).value||'', dp:($('dp')||{}).value||'', tenor:($('tenor')||{}).value||'',
          metode:selText('metode'), bunga:($('bunga')||{}).value||'',
          totalBunga:($('totalBunga')||{}).textContent||'', totalBiaya:($('totalBiaya')||{}).textContent||'',
          totalBayar:($('totalBayar')||{}).textContent||'', nilai:($('nilaiDibiayai')||{}).textContent||'' };
      }

      function renderGround(){
        var d=readData();
        var ctx='<div class="aic-ctx"><i class="ti ti-cash"></i> Paylater \u00b7 Cicilan <b>'+esc(d.cicilanTxt)+'</b>/bln'
          +(d.apr?' \u00b7 APR <b>~'+esc(d.apr.replace('/tahun','/th'))+'</b>':'')
          +(d.ratio!=null?' \u00b7 DSR <b>'+d.ratio.toFixed(1)+'%</b>':'')+'</div>';
        if(d.ratio==null){ ground.innerHTML=ctx+'<div class="aic-nogauge">Isi "Penghasilan Bulanan" lalu hitung ulang untuk lihat gauge risiko cicilan.</div>'; return; }
        var pos=Math.max(0,Math.min(d.ratio,50))/50*100;
        var vc=d.ratio<=30?'good':(d.ratio<=40?'warn':'bad');
        var vi=d.ratio<=30?'ti-circle-check':(d.ratio<=40?'ti-alert-triangle':'ti-alert-circle');
        var vt=d.ratio<=30?'Aman \u2014 cicilan di bawah 30% penghasilan':(d.ratio<=40?'Agak ketat \u2014 tapi masih bisa diatur':'Cukup berat \u2014 pertimbangkan lagi');
        ground.innerHTML=ctx
          +'<div class="aic-gauge"><div class="aic-gauge-mark" style="left:'+pos.toFixed(1)+'%"><span>kamu \u00b7 '+d.ratio.toFixed(0)+'%</span></div>'
          +'<div class="aic-gauge-bar"><div class="z-green"></div><div class="z-amber"></div><div class="z-red"></div></div>'
          +'<div class="aic-gauge-lbl"><span class="g">Aman &le;30%</span><span class="a">Hati-hati</span><span class="r">Berat &ge;40%</span></div></div>'
          +'<div class="aic-verdict '+vc+'"><i class="ti '+vi+'"></i> '+vt+'</div>';
      }

      function buildContext(){
        var d=readData();
        var p=[
          'Kalkulator: Paylater / cicilan BNPL (Buy Now Pay Later).',
          d.harga?('Harga barang/jasa: Rp '+d.harga+'.'):'',
          (d.dp && d.dp!=='0')?('Uang muka (DP): Rp '+d.dp+'.'):'',
          'Nilai dibiayai: '+d.nilai+'. Cicilan per bulan: '+d.cicilanTxt+'.',
          'Tenor: '+d.tenor+' bulan. Metode: '+d.metode+'. Bunga: '+d.bunga+'% per bulan.',
          'Total bunga: '+d.totalBunga+'. Total biaya+admin: '+d.totalBiaya+'. Total bayar: '+d.totalBayar+'.',
          d.apr?('Estimasi APR efektif (bunga setahun sebenarnya): ~'+d.apr+'.'):'',
          d.income?('Penghasilan bulanan: Rp '+d.income+'.'):'',
          (d.cicLain && d.cicLain!=='0')?('Cicilan lain per bulan: Rp '+d.cicLain+'.'):'',
          d.ratio!=null?('Rasio cicilan terhadap penghasilan (DSR): '+d.ratio.toFixed(1)+'%.'):'',
          'Acuan untuk jawabanmu: DSR (total cicilan dibanding penghasilan) idealnya maksimal 30%, di atas 40% berat. Aturan OJK POJK 32/2025 tentang BNPL: bunga konsumtif dibatasi 0,1% per hari sejak 1 Januari 2026, dan total kewajiban dibatasi maksimal 100% dari pokok (Lock Cap, tidak boleh lebih dari 2x pokok). "Bunga flat" yang terlihat kecil per bulan bisa berarti APR efektif sangat tinggi karena dihitung dari pokok awal. Galbay (gagal bayar) menimbulkan denda yang menumpuk dan catatan negatif di SLIK OJK yang menyulitkan pengajuan kredit lain. Bandingkan selalu dengan harga tunai; cicilan kecil belum tentu lebih murah. Pakai paylater untuk kebutuhan, bukan keinginan impulsif. Jawab ringkas, hangat, pakai sapaan "kamu", dan jangan mengarang angka di luar data di atas.'
        ];
        return p.filter(Boolean).join('\n');
      }

      function mdInline(t){ t=t.replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>'); t=t.replace(/__([^_]+)__/g,'<strong>$1</strong>'); t=t.replace(/(^|[\s(])\*([^*\n]+)\*(?=[\s).,!?:;]|$)/g,'$1<em>$2</em>'); t=t.replace(/`([^`]+)`/g,'<code>$1</code>'); return t; }
      function mdToHtml(raw){ var lines=String(raw).replace(/\r/g,'').split('\n'); var out=[]; for(var i=0;i<lines.length;i++){ var t=lines[i].trim(); if(t==='')continue; if(/^([-*_]\s*){3,}$/.test(t))continue; var h=t.match(/^#{1,6}\s+(.*)$/); if(h){out.push('<p class="aic-h">'+mdInline(esc(h[1]))+'</p>');continue;} var b=t.match(/^[-*\u2022]\s+(.*)$/); if(b){out.push('<div class="aic-li"><span class="aic-mk">\u2022</span><span>'+mdInline(esc(b[1]))+'</span></div>');continue;} var n=t.match(/^(\d+)[.)]\s+(.*)$/); if(n){out.push('<div class="aic-li"><span class="aic-mk">'+n[1]+'.</span><span>'+mdInline(esc(n[2]))+'</span></div>');continue;} out.push('<p>'+mdInline(esc(t))+'</p>'); } return out.join('')||('<p>'+mdInline(esc(String(raw)))+'</p>'); }
      function typeInto(bubble, text, done){
        var n=0, len=text.length, step=Math.max(2, Math.round(len/90));
        var t=setInterval(function(){
          n=Math.min(len, n+step);
          bubble.innerHTML=mdToHtml(text.slice(0,n));
          msgs.scrollTop=msgs.scrollHeight;
          if(n>=len){ clearInterval(t); done&&done(); }
        }, 18);
      }
      function mkCopyBtn(text){
        var b=document.createElement('button'); b.type='button'; b.className='aic-copy';
        b.innerHTML='<i class="ti ti-copy"></i><span>Salin</span>';
        b.addEventListener('click', function(){ HC.copyText(text, 'Jawaban tersalin!'); });
        return b;
      }
      function addMsgTyped(text){
        var row=document.createElement('div'); row.className='aic-row ai';
        row.innerHTML='<div class="aic-av"><i class="ti ti-sparkles"></i></div><div class="aic-bub aic-rich"></div>';
        var bub=row.querySelector('.aic-bub'); msgs.appendChild(row); msgs.scrollTop=msgs.scrollHeight;
        typeInto(bub, text, function(){ bub.appendChild(mkCopyBtn(text)); renderFollowups(); });
        return row;
      }
      var FOLLOWUPS=['Worth it gak cicilan ini?','Mending cash atau paylater?','Bunga segini wajar gak?','APR-nya kok bisa setinggi itu?','Mending tenor pendek atau panjang?','Kalau telat bayar gimana?','Cicilan ini berat gak buat gajiku?','Aman gak buat keuangan bulananku?'];
      function renderFollowups(){
        chips.innerHTML='';
        var asked=history.filter(function(m){return m.role==='user';}).map(function(m){return m.content;});
        var pool=FOLLOWUPS.filter(function(q){return asked.indexOf(q)<0;});
        pool.slice(0,3).forEach(function(q){
          var b=document.createElement('button'); b.type='button'; b.className='aic-chip'; b.textContent=q;
          b.addEventListener('click', function(){ ask(q); }); chips.appendChild(b);
        });
      }
      function addMsg(role,text){ var row=document.createElement('div'); row.className='aic-row '+role; if(role==='ai'){ row.innerHTML='<div class="aic-av"><i class="ti ti-sparkles"></i></div><div class="aic-bub aic-rich"></div>'; row.querySelector('.aic-bub').innerHTML=mdToHtml(text); } else { row.innerHTML='<div class="aic-bub"></div>'; row.querySelector('.aic-bub').textContent=text; } msgs.appendChild(row); msgs.scrollTop=msgs.scrollHeight; return row; }
      function addTyping(){ var row=document.createElement('div'); row.className='aic-row ai'; row.innerHTML='<div class="aic-av"><i class="ti ti-sparkles"></i></div><div class="aic-bub aic-typing"><span></span><span></span><span></span></div>'; msgs.appendChild(row); msgs.scrollTop=msgs.scrollHeight; return row; }
      function todayKey(){ var d=new Date(); return 'hc-coach-'+d.getFullYear()+'-'+(d.getMonth()+1)+'-'+d.getDate(); }
      function getCount(){ try{ return parseInt(localStorage.getItem(todayKey())||'0',10)||0; }catch(e){ return 0; } }
      function incCount(){ try{ localStorage.setItem(todayKey(), getCount()+1); }catch(e){} }
      function refreshLimit(){ var left=Math.max(0,LIMIT-getCount()); limitEl.textContent='Sisa '+left+' pertanyaan gratis hari ini.'; var done=left<=0; input.disabled=done; send.disabled=done; if(done){ input.placeholder='Batas harian tercapai, balik lagi besok ya.'; } }
      var busy=false;
      async function ask(text){ text=(text||'').trim(); if(!text||busy)return; if(getCount()>=LIMIT){refreshLimit();return;} busy=true; send.disabled=true; addMsg('user',text); history.push({role:'user',content:text}); input.value=''; chips.innerHTML=''; var typing=addTyping(); incCount();
        try{ var res=await fetch('/api/coach',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({calcContext:buildContext(),messages:history})}); var data=await res.json().catch(function(){return {error:'Respon tidak valid.'};}); typing.parentNode&&typing.remove(); if(data&&data.reply){ addMsgTyped(data.reply); history.push({role:'assistant',content:data.reply}); } else { addMsg('ai','\u26a0\ufe0f '+((data&&data.error)||'Coach lagi sibuk, coba lagi sebentar ya.')); } }
        catch(e){ typing.parentNode&&typing.remove(); addMsg('ai','\u26a0\ufe0f Gagal terhubung ke Coach. Coba lagi ya.'); }
        busy=false; refreshLimit(); if(!input.disabled) input.focus(); }
      var PRESETS=['Worth it gak cicilan ini?','Mending cash atau paylater?','Bunga segini wajar gak?'];
      function renderChips(){ chips.innerHTML=''; PRESETS.forEach(function(q){ var b=document.createElement('button'); b.type='button'; b.className='aic-chip'; b.textContent=q; b.addEventListener('click',function(){ask(q);}); chips.appendChild(b); }); }
      var opened=false;
      trigger.addEventListener('click', function(){ if(panel.hasAttribute('hidden')){ panel.removeAttribute('hidden'); wrap.classList.add('open'); applyTheme(); renderGround(); refreshLimit(); if(!opened){renderChips();opened=true;} } else { panel.setAttribute('hidden',''); wrap.classList.remove('open'); } });
      send.addEventListener('click', function(){ ask(input.value); });
      input.addEventListener('keydown', function(e){ if(e.key==='Enter'){ e.preventDefault(); ask(input.value); } });
      var bh=$('btnHitung'); if(bh){ bh.addEventListener('click', function(){ setTimeout(function(){ if(!panel.hasAttribute('hidden')) renderGround(); },800); }); }
    });
  })();
</script>
</Layout>

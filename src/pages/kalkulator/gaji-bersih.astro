---
import Layout from '../../layouts/Layout.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';
import FAQ from '../../components/FAQ.astro';
import AdSlot from '../../components/AdSlot.astro';
import CalculatorCard from '../../components/CalculatorCard.astro';
import { getCalculatorBySlug, getRelatedCalculators } from '../../data/calculators';

const calc = getCalculatorBySlug('gaji-bersih')!;
const related = getRelatedCalculators('gaji-bersih', 3);

const faqs = [
  { question: 'Apa itu gaji bersih (take-home pay)?', answer: 'Gaji bersih = uang yang benar-benar masuk rekeningmu setelah semua potongan: BPJS Kesehatan (1%), BPJS JHT (2%), BPJS JP (1%), PPh 21 progresif, dan potongan internal (koperasi, cicilan). Biasanya 70-85% dari gaji bruto.' },
  { question: 'Berapa total potongan BPJS karyawan?', answer: 'Total potongan BPJS karyawan = 4% gaji: <strong>BPJS Kesehatan 1%</strong> (maks Rp 120rb/bln, batas ceiling Rp 12jt), <strong>JHT 2%</strong>, <strong>JP 1%</strong> (maks Rp 87rb/bln, batas ceiling Rp 8,7jt). Perusahaan tambah ~9% dari sisinya untuk JKK, JKM, BPJS Kes.' },
  { question: 'Bagaimana alokasi 50/30/20 yang tepat?', answer: 'Aturan emas alokasi take-home pay: <strong>50% kebutuhan</strong> (sewa/cicilan, listrik, transport, makan), <strong>30% keinginan</strong> (hiburan, hobi, makan di luar), <strong>20% tabungan & investasi</strong> (dana darurat, reksadana, emas).' },
  { question: 'Apakah lembur dan komisi kena PPh?', answer: 'YA, lembur dan komisi termasuk objek PPh 21. Hitungnya ditambahkan ke bruto setahun. Tarifnya progresif sama seperti gaji pokok. Komisi yang besar bisa bikin loncat ke lapisan tarif berikutnya.' },
];

const bcItems = [{ label: 'Beranda', href: '/' }, { label: 'Kalkulator', href: '/#tools' }, { label: calc.shortTitle }];
---

<Layout title={calc.title} description={calc.metaDescription} keywords={calc.keywords} canonical={`https://hitungcerdas.net/kalkulator/${calc.slug}`}>
  <section class="calc-hero" style="--cat-color:#059669;--cat-color2:#047857">
    <div class="wrap">
      <Breadcrumb items={bcItems} />
      <div class="calc-hero-grid">
        <div class="calc-hero-text">
          <div class="calc-hero-tag" style="background:#D1FAE5;color:#047857"><i class="ti ti-wallet"></i> {calc.categoryLabel}</div>
          <h1 class="calc-hero-title">{calc.h1}</h1>
          <p class="calc-hero-sub">{calc.introText}</p>
          <ul class="calc-hero-benefits">{calc.benefits.map(b => <li><i class="ti ti-check"></i> {b}</li>)}</ul>
        </div>
        <div class="calc-hero-illust">
          <svg viewBox="0 0 200 200" fill="none">
            <circle cx="100" cy="100" r="85" fill="rgba(255,255,255,.08)"/>
            <rect x="50" y="65" width="100" height="70" rx="8" fill="#FFF" stroke="#FBBF24" stroke-width="2"/>
            <circle cx="100" cy="100" r="20" fill="none" stroke="#059669" stroke-width="3"/>
            <text x="100" y="106" text-anchor="middle" font-size="14" font-weight="900" fill="#059669" font-family="Inter">Rp</text>
            <rect x="60" y="75" width="20" height="5" fill="#FBBF24"/>
            <rect x="120" y="75" width="20" height="5" fill="#FBBF24"/>
          </svg>
        </div>
      </div>
    </div>
  </section>

  <AdSlot format="horizontal" />

  <section class="calc-section">
    <div class="wrap">
      <div class="calc-card">
        <div class="calc-card-hdr">
          <div class="calc-card-hdr-ic" style="background:linear-gradient(135deg,#059669 0%,#047857 100%)"><i class="ti ti-wallet"></i></div>
          <div><h2 class="calc-card-title">Kalkulator Gaji Bersih</h2><p class="calc-card-sub">Hitung take-home pay setelah BPJS & PPh 21</p></div>
        </div>

        <div class="calc-grid">
          <div class="calc-input">
            <div class="field"><label>Gaji Pokok</label>
              <div class="input-wrap"><span class="input-prefix">Rp</span>
                <input type="text" id="gaji" class="has-prefix" value="8.000.000" inputmode="numeric" /></div></div>

            <div class="field"><label>Tunjangan Tetap <span class="hint">(transport, makan)</span></label>
              <div class="input-wrap"><span class="input-prefix">Rp</span>
                <input type="text" id="tunj" class="has-prefix" value="1.500.000" inputmode="numeric" /></div></div>

            <div class="field"><label>Tunjangan Tidak Tetap <span class="hint">(lembur, komisi)</span></label>
              <div class="input-wrap"><span class="input-prefix">Rp</span>
                <input type="text" id="tunjVar" class="has-prefix" value="0" inputmode="numeric" /></div></div>

            <div class="field"><label>Status PTKP</label>
              <select id="ptkp">
                <option value="54000000">TK/0 - Lajang</option>
                <option value="58500000">K/0 - Menikah no anak</option>
                <option value="63000000">K/1 - 1 anak</option>
                <option value="67500000">K/2 - 2 anak</option>
                <option value="72000000">K/3 - 3 anak</option>
              </select></div>

            <div class="field"><label>Potongan Lain per Bulan <span class="hint">(koperasi, cicilan internal)</span></label>
              <div class="input-wrap"><span class="input-prefix">Rp</span>
                <input type="text" id="potLain" class="has-prefix" value="0" inputmode="numeric" /></div></div>

            <div class="calc-action-bar">
              <button type="button" class="btn-hitung" id="btnHitung"><i class="ti ti-calculator"></i> Hitung Sekarang</button>
              <button type="button" class="btn-reset" id="btnReset"><i class="ti ti-refresh"></i> Reset</button>
            </div>
          </div>

          <div class="calc-output">
            <div class="result-placeholder" id="placeholder">
              <i class="ti ti-wallet result-placeholder-icon"></i>
              <div class="result-placeholder-title">Take-Home Pay akan muncul di sini</div>
              <div class="result-placeholder-desc">Isi komponen gaji, klik <strong>"Hitung Sekarang"</strong>.</div>
            </div>

            <div class="result-wrapper" id="results">
              <div class="result-main">
                <div class="result-label">Take-Home Pay (Gaji Bersih)</div>
                <div class="result-value" id="thp">Rp 0</div>
                <div class="result-meta"><i class="ti ti-info-circle"></i> <span id="thpPct">0% dari bruto</span></div>
              </div>

              <div class="result-grid">
                <div class="result-item"><div class="result-item-label">Gaji Bruto/Bulan</div><div class="result-item-value" id="bruto">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">BPJS Karyawan</div><div class="result-item-value result-warn" id="bpjsTotal">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">PPh 21/Bulan</div><div class="result-item-value result-warn" id="pphBulan">Rp 0</div></div>
                <div class="result-item"><div class="result-item-label">Potongan Lain</div><div class="result-item-value" id="potLainShow">Rp 0</div></div>
              </div>

              <div class="adv-section">
                <div class="adv-toggle"><span><i class="ti ti-pie-chart"></i>&nbsp; Alokasi 50/30/20 Take-Home</span><i class="ti ti-chevron-down chev"></i></div>
                <div class="adv-content">
                  <table class="compare-table">
                    <thead><tr><th>Kategori</th><th class="right">% THP</th><th class="right">Nominal/Bulan</th></tr></thead>
                    <tbody id="alokasiBody"></tbody>
                  </table>
                  <p style="font-size:11.5px;color:var(--text-mute);margin-top:8px;line-height:1.5">💡 Aturan klasik: <strong>50%</strong> kebutuhan (cicilan, makan, transport), <strong>30%</strong> keinginan (hiburan), <strong>20%</strong> tabungan & investasi.</p>
                </div>
              </div>

              <div class="adv-section">
                <div class="adv-toggle"><span><i class="ti ti-target"></i>&nbsp; Tabungan 20% Selama 5 Tahun</span><i class="ti ti-chevron-down chev"></i></div>
                <div class="adv-content">
                  <table class="compare-table">
                    <thead><tr><th>Tahun</th><th class="right">Tabungan Pokok</th><th class="right">+ Bunga 6%/thn</th></tr></thead>
                    <tbody id="proyeksiBody"></tbody>
                  </table>
                </div>
              </div>

              <div class="export-row">
                <button type="button" class="export-btn img" id="exportImg"><i class="ti ti-photo-down"></i><span>Save Image</span></button>
                <button type="button" class="export-btn wa" id="exportWa"><i class="ti ti-brand-whatsapp"></i><span>Share WA</span></button>
                <button type="button" class="export-btn pdf" id="exportPdf"><i class="ti ti-printer"></i><span>Print/PDF</span></button>
                <button type="button" class="export-btn copy" id="exportCopy"><i class="ti ti-copy"></i><span>Copy Teks</span></button>
              </div>

              <!-- ===== AI COACH KEPUTUSAN (mandiri) ===== -->
              <div class="aic-wrap" id="aicWrap">
                <button type="button" class="aic-trigger" id="aicTrigger">
                  <span class="aic-trigger-ic"><i class="ti ti-sparkles"></i></span>
                  <span class="aic-trigger-txt">
                    <strong>Tanya AI Coach</strong>
                    <small>Bingung sama hasilnya? Tanya di sini</small>
                  </span>
                  <i class="ti ti-chevron-down aic-trigger-arr"></i>
                </button>
                <div class="aic-panel" id="aicPanel" hidden>
                  <div class="aic-head">
                    <span class="aic-head-ic"><i class="ti ti-sparkles"></i></span>
                    <div class="aic-head-tt">
                      <strong>AI Coach Keputusan</strong>
                      <small>Bantu pahami gaji bersihmu</small>
                    </div>
                    <span class="aic-badge">BETA</span>
                  </div>
                  <div class="aic-ground" id="aicGround"></div>
                  <div class="aic-msgs" id="aicMsgs"></div>
                  <div class="aic-chips" id="aicChips"></div>
                  <div class="aic-input">
                    <input type="text" id="aicText" placeholder="Tanya soal gaji bersihmu..." maxlength="600" autocomplete="off" />
                    <button type="button" id="aicSend" aria-label="Kirim"><i class="ti ti-arrow-up"></i></button>
                  </div>
                  <div class="aic-disc">
                    <i class="ti ti-info-circle"></i>
                    <span>Estimasi edukatif berbasis hasil hitunganmu, bukan nasihat keuangan berlisensi.</span>
                  </div>
                  <div class="aic-limit" id="aicLimit"></div>
                </div>
              </div>
              <!-- ===== /AI COACH ===== -->

            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="content-section">
    <div class="wrap-text">
      <h2>Cara Menghitung Gaji Bersih Karyawan</h2>
      <p>Gaji bersih (take-home pay) = gaji bruto - BPJS Kesehatan 1% - JHT 2% - JP 1% - PPh 21 progresif - potongan lain. Untuk gaji bruto Rp 9,5jt, THP biasanya sekitar Rp 8,8jt (93%). Mau tahu rincian potongan pajaknya? Hitung terpisah di <a href="/kalkulator/pph-21">kalkulator PPh 21</a>. Penghasilanmu sudah capai nisab? Cek juga <a href="/kalkulator/zakat-penghasilan">zakat penghasilan</a>.</p>
      <div class="cta-box"><h3>Coba Sekarang</h3><p>Isi data gaji & potongan, klik "Hitung Sekarang" untuk simulasi lengkap dengan alokasi 50/30/20.</p></div>
    </div>
  </section>

  <AdSlot format="horizontal" />
  <FAQ title="Pertanyaan Seputar Gaji Bersih" items={faqs} />

  <section class="section"><div class="wrap">
    <div class="section-hdr"><div class="section-eyebrow">Kalkulator Lainnya</div><h2 class="section-title">Hitung Juga yang Lain</h2></div>
    <div class="related-grid">{related.map(c => <CalculatorCard calc={c} />)}</div>
  </div></section>
  <p style="text-align:center;font-size:.85rem;color:#64748b;margin:1.2rem auto;max-width:600px;">
    📅 Tarif diperbarui <strong>Juni 2026</strong> · Sumber: DJP (UU HPP) & BPJS
  </p>
<style is:global>
  .aic-wrap{margin-top:18px}
  .aic-trigger{width:100%;display:flex;align-items:center;gap:12px;background:#EFF4FF;border:1px solid #C7DBFF;border-radius:14px;padding:13px 15px;cursor:pointer;text-align:left;transition:background .15s;font-family:inherit}
  .aic-trigger:hover{background:#E4EDFF}
  .aic-trigger-ic{width:36px;height:36px;flex-shrink:0;border-radius:50%;background:#2563EB;color:#fff;display:flex;align-items:center;justify-content:center;font-size:19px}
  .aic-trigger-txt{flex:1;display:flex;flex-direction:column;line-height:1.25}
  .aic-trigger-txt strong{font-size:14px;color:#0A2540}
  .aic-trigger-txt small{font-size:12px;color:#64748B}
  .aic-trigger-arr{font-size:18px;color:#64748B;transition:transform .2s}
  .aic-wrap.open .aic-trigger-arr{transform:rotate(180deg)}

  .aic-panel{margin-top:10px;background:#fff;border:1px solid #E5EAF1;border-radius:16px;overflow:hidden;animation:aicIn .2s ease}
  @keyframes aicIn{from{opacity:0;transform:translateY(-6px)}to{opacity:1;transform:none}}
  .aic-head{display:flex;align-items:center;gap:10px;padding:12px 14px;border-bottom:1px solid #E5EAF1}
  .aic-head-ic{width:30px;height:30px;border-radius:50%;background:#EFF4FF;color:#2563EB;display:flex;align-items:center;justify-content:center;font-size:16px}
  .aic-head-tt{flex:1;display:flex;flex-direction:column;line-height:1.2}
  .aic-head-tt strong{font-size:14px;color:#0A2540}
  .aic-head-tt small{font-size:11px;color:#64748B}
  .aic-badge{font-size:10px;font-weight:700;background:#EFF4FF;color:#2563EB;padding:2px 8px;border-radius:8px}

  .aic-ground{padding:12px 14px;border-bottom:1px solid #E5EAF1;background:#F8FAFC}
  .aic-ctx{font-size:12px;color:#475569;margin-bottom:10px}
  .aic-ctx b{color:#0A2540;font-weight:700}
  .aic-gauge{position:relative;padding-top:19px}
  .aic-gauge-mark{position:absolute;top:0;z-index:3}
  .aic-gauge-mark span{display:block;font-size:10px;font-weight:700;background:#0A2540;color:#fff;padding:1px 7px;border-radius:9px;white-space:nowrap;transform:translateX(-50%)}
  .aic-gauge-mark::after{content:"";position:absolute;left:0;top:15px;width:2px;height:15px;background:#0A2540}
  .aic-gauge-bar{display:flex;height:10px;border-radius:5px;overflow:hidden}
  .aic-gauge-bar .z-green{flex:0 0 60%;background:#D1FAE5}
  .aic-gauge-bar .z-amber{flex:0 0 20%;background:#FEF3C7}
  .aic-gauge-bar .z-red{flex:0 0 20%;background:#FEE2E2}
  .aic-gauge-lbl{display:flex;font-size:10px;margin-top:5px}
  .aic-gauge-lbl .g{flex:0 0 60%;color:#059669}
  .aic-gauge-lbl .a{flex:0 0 20%;text-align:center;color:#D97706;font-weight:600}
  .aic-gauge-lbl .r{flex:0 0 20%;text-align:right;color:#DC2626}
  .aic-verdict{font-size:12.5px;font-weight:600;margin-top:11px;display:flex;align-items:center;gap:6px}
  .aic-verdict.good{color:#059669}
  .aic-verdict.warn{color:#D97706}
  .aic-verdict.bad{color:#DC2626}
  .aic-nogauge{font-size:11px;color:#94A3B8;padding:4px 0}

  .aic-msgs{padding:12px 14px;display:flex;flex-direction:column;gap:10px;max-height:320px;overflow-y:auto}
  .aic-msgs:empty{display:none}
  .aic-row{display:flex;gap:8px;align-items:flex-start}
  .aic-row.user{justify-content:flex-end}
  .aic-av{width:24px;height:24px;flex-shrink:0;border-radius:50%;background:#EFF4FF;color:#2563EB;display:flex;align-items:center;justify-content:center;font-size:13px}
  .aic-bub{max-width:82%;font-size:13px;line-height:1.55;border-radius:13px;padding:9px 12px;white-space:pre-wrap;word-wrap:break-word;overflow-wrap:anywhere}
  .aic-row.ai .aic-bub{background:#F1F5F9;color:#0A2540}
  .aic-row.user .aic-bub{background:#2563EB;color:#fff}
  .aic-bub.aic-rich{white-space:normal}
  .aic-bub.aic-rich p{margin:0 0 7px}
  .aic-bub.aic-rich p:last-child{margin-bottom:0}
  .aic-bub.aic-rich p.aic-h{font-weight:700;margin:10px 0 5px}
  .aic-bub.aic-rich p.aic-h:first-child{margin-top:0}
  .aic-bub.aic-rich strong{font-weight:700}
  .aic-bub.aic-rich em{font-style:italic}
  .aic-bub.aic-rich code{background:rgba(0,0,0,.06);padding:1px 5px;border-radius:4px;font-size:12px}
  .aic-bub.aic-rich .aic-li{display:flex;gap:7px;margin:4px 0;align-items:flex-start}
  .aic-bub.aic-rich .aic-li:first-child{margin-top:0}
  .aic-bub.aic-rich .aic-mk{flex-shrink:0;color:#2563EB;font-weight:700;min-width:15px}
  .aic-typing span{display:inline-block;width:6px;height:6px;border-radius:50%;background:#94A3B8;margin:0 1px;animation:aicblink 1.2s infinite}
  .aic-typing span:nth-child(2){animation-delay:.2s}
  .aic-typing span:nth-child(3){animation-delay:.4s}
  @keyframes aicblink{0%,60%,100%{opacity:.3}30%{opacity:1}}

  .aic-chips{display:flex;flex-wrap:wrap;gap:6px;padding:0 14px 12px}
  .aic-chips:empty{display:none}
  .aic-chip{font-size:11px;background:#EFF4FF;color:#2563EB;border:1px solid #D6E4FF;border-radius:14px;padding:6px 11px;cursor:pointer;font-family:inherit}
  .aic-chip:hover{background:#E0EBFF}

  .aic-input{display:flex;align-items:center;gap:8px;padding:10px 12px;border-top:1px solid #E5EAF1}
  .aic-input input{flex:1;border:1px solid #E5EAF1;border-radius:18px;padding:9px 14px;font-size:13px;background:#F8FAFC;color:#0A2540;outline:none;font-family:inherit}
  .aic-input input:focus{border-color:#2563EB}
  .aic-input button{width:34px;height:34px;flex-shrink:0;border:0;border-radius:50%;background:#2563EB;color:#fff;cursor:pointer;display:flex;align-items:center;justify-content:center;font-size:17px}
  .aic-input button:disabled{background:#CBD5E1;cursor:not-allowed}

  .aic-disc{display:flex;gap:6px;align-items:flex-start;padding:8px 14px;font-size:10.5px;color:#94A3B8;line-height:1.45;border-top:1px solid #F1F5F9}
  .aic-disc i{margin-top:1px}
  .aic-limit{padding:0 14px 10px;font-size:11px;color:#64748B}

  /* ===== DARK MODE (mandiri, via .aic-dark) ===== */
  .aic-dark .aic-trigger{background:#16263B;border-color:#2A445F}
  .aic-dark .aic-trigger:hover{background:#1B2E47}
  .aic-dark .aic-trigger-txt strong{color:#E8EEF6}
  .aic-dark .aic-trigger-txt small{color:#93A4B8}
  .aic-dark .aic-trigger-arr{color:#93A4B8}
  .aic-dark .aic-panel{background:#0E1A2B;border-color:#243A56}
  .aic-dark .aic-head{border-color:#243A56}
  .aic-dark .aic-head-ic{background:#172A44;color:#8FB4FF}
  .aic-dark .aic-head-tt strong{color:#E8EEF6}
  .aic-dark .aic-head-tt small{color:#93A4B8}
  .aic-dark .aic-badge{background:#172A44;color:#8FB4FF}
  .aic-dark .aic-ground{background:#122036;border-color:#243A56}
  .aic-dark .aic-ctx{color:#A9B7C9}
  .aic-dark .aic-ctx b{color:#E8EEF6}
  .aic-dark .aic-gauge-mark span{background:#E8EEF6;color:#0E1A2B}
  .aic-dark .aic-gauge-mark::after{background:#E8EEF6}
  .aic-dark .aic-gauge-bar .z-green{background:rgba(16,185,129,.24)}
  .aic-dark .aic-gauge-bar .z-amber{background:rgba(245,158,11,.24)}
  .aic-dark .aic-gauge-bar .z-red{background:rgba(239,68,68,.24)}
  .aic-dark .aic-gauge-lbl .g{color:#34D399}
  .aic-dark .aic-gauge-lbl .a{color:#FBBF24}
  .aic-dark .aic-gauge-lbl .r{color:#F87171}
  .aic-dark .aic-verdict.good{color:#34D399}
  .aic-dark .aic-verdict.warn{color:#FBBF24}
  .aic-dark .aic-verdict.bad{color:#F87171}
  .aic-dark .aic-av{background:#172A44;color:#8FB4FF}
  .aic-dark .aic-row.ai .aic-bub{background:#1B2C44;color:#E8EEF6}
  .aic-dark .aic-bub.aic-rich .aic-mk{color:#8FB4FF}
  .aic-dark .aic-bub.aic-rich code{background:rgba(255,255,255,.1)}
  .aic-dark .aic-chip{background:rgba(37,99,235,.16);color:#8FB4FF;border-color:#2A445F}
  .aic-dark .aic-chip:hover{background:rgba(37,99,235,.26)}
  .aic-dark .aic-input{border-color:#243A56}
  .aic-dark .aic-input input{background:#16263B;border-color:#2A445F;color:#E8EEF6}
  .aic-dark .aic-input input:focus{border-color:#3B82F6}
  .aic-dark .aic-disc{color:#7E8DA1;border-color:#1B2C44}
  .aic-dark .aic-limit{color:#93A4B8}

  /* ==== AI Coach: composition / progress gauge ==== */
  .aic-stack{display:flex;height:24px;border-radius:6px;overflow:hidden;margin-top:2px}
  .aic-stack .seg{display:flex;align-items:center;justify-content:center;min-width:0}
  .aic-stack .seg-lbl{font-size:10px;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;padding:0 5px}
  .aic-stack .s-green{background:#10B981}.aic-stack .s-green .seg-lbl{color:#fff}
  .aic-stack .s-amber{background:#FDE68A}.aic-stack .s-amber .seg-lbl{color:#92400E}
  .aic-stack .s-red{background:#FCA5A5}.aic-stack .s-red .seg-lbl{color:#7F1D1D}
  .aic-stack .s-blue{background:#DBEAFE}.aic-stack .s-blue .seg-lbl{color:#1E40AF}
  .aic-stack .s-grey{background:#E2E8F0}.aic-stack .s-grey .seg-lbl{color:#475569}
  .aic-legend{display:flex;flex-wrap:wrap;gap:14px;margin-top:9px;font-size:11px;color:#475569}
  .aic-legend .dot{display:inline-block;width:9px;height:9px;border-radius:2px;margin-right:6px;vertical-align:middle}
  .aic-legend b{color:#0A2540;font-weight:700}
  .aic-verdict.info{color:#2563EB}
  .aic-verdict b{font-weight:700}
  .aic-dark .aic-stack .s-amber{background:rgba(245,158,11,.30)}.aic-dark .aic-stack .s-amber .seg-lbl{color:#FCD34D}
  .aic-dark .aic-stack .s-red{background:rgba(239,68,68,.32)}.aic-dark .aic-stack .s-red .seg-lbl{color:#FCA5A5}
  .aic-dark .aic-stack .s-blue{background:#24406B}.aic-dark .aic-stack .s-blue .seg-lbl{color:#BFD4FF}
  .aic-dark .aic-stack .s-grey{background:#334155}.aic-dark .aic-stack .s-grey .seg-lbl{color:#CBD5E1}
  .aic-dark .aic-legend{color:#A9B7C9}.aic-dark .aic-legend b{color:#E8EEF6}
  .aic-dark .aic-verdict.info{color:#8FB4FF}
</style>
</Layout>

<script is:inline>
  (function() {
    function ready(fn) { if (window.HC && window.html2canvas) fn(); else setTimeout(() => ready(fn), 100); }
    ready(() => {
      const $ = id => document.getElementById(id);
      const els = { gaji: $('gaji'), tunj: $('tunj'), tunjVar: $('tunjVar'), ptkp: $('ptkp'), potLain: $('potLain') };
      [els.gaji, els.tunj, els.tunjVar, els.potLain].forEach(el => el.addEventListener('input', HC.fmtInput));
      document.querySelectorAll('.adv-toggle').forEach(t => t.addEventListener('click', () => t.parentElement.classList.toggle('open')));

      const BRACKETS = [
        { max: 60000000, rate: 0.05 }, { max: 250000000, rate: 0.15 },
        { max: 500000000, rate: 0.25 }, { max: 5000000000, rate: 0.30 },
        { max: Infinity, rate: 0.35 },
      ];
      function calcPPh(pkp) {
        if (pkp <= 0) return 0;
        let r = pkp, total = 0, prev = 0;
        for (const b of BRACKETS) { if (r <= 0) break; const s = Math.min(r, b.max - prev); total += s * b.rate; r -= s; prev = b.max; }
        return total;
      }

      let lastCalc = null;

      function hitung() {
        const gaji = HC.parseRp(els.gaji.value);
        const tunj = HC.parseRp(els.tunj.value);
        const tunjVar = HC.parseRp(els.tunjVar.value);
        const ptkp = parseInt(els.ptkp.value);
        const potLain = HC.parseRp(els.potLain.value);

        if (gaji === 0) { HC.toast('Masukkan gaji pokok dulu', 'error'); return; }

        const brutoBulan = gaji + tunj + tunjVar;
        // BPJS (with ceiling rules)
        const bpjsKes = Math.min(brutoBulan * 0.01, 120000);
        const bpjsJht = brutoBulan * 0.02;
        const bpjsJp = Math.min(brutoBulan * 0.01, 87000);
        const bpjsTotal = bpjsKes + bpjsJht + bpjsJp;

        // PPh 21
        const brutoTahun = brutoBulan * 12;
        const biayaJabatan = Math.min(brutoTahun * 0.05, 6000000);
        const neto = brutoTahun - biayaJabatan - (bpjsTotal * 12);
        const pkp = Math.max(0, neto - ptkp);
        const pphTahun = calcPPh(pkp);
        const pphBulan = pphTahun / 12;

        const thp = brutoBulan - bpjsTotal - pphBulan - potLain;
        const thpPct = (thp / brutoBulan) * 100;

        $('thp').textContent = HC.fmt(thp);
        $('thpPct').textContent = `${thpPct.toFixed(1)}% dari bruto`;
        $('bruto').textContent = HC.fmt(brutoBulan);
        $('bpjsTotal').textContent = HC.fmt(bpjsTotal);
        $('pphBulan').textContent = HC.fmt(pphBulan);
        $('potLainShow').textContent = HC.fmt(potLain);

        $('alokasiBody').innerHTML = `
          <tr><td>🏠 Kebutuhan (50%)</td><td class="right">50%</td><td class="right">${HC.fmt(thp * 0.5)}</td></tr>
          <tr><td>🎮 Keinginan (30%)</td><td class="right">30%</td><td class="right">${HC.fmt(thp * 0.3)}</td></tr>
          <tr class="active"><td>💰 Tabungan (20%)</td><td class="right">20%</td><td class="right">${HC.fmt(thp * 0.2)}</td></tr>`;

        // Projection
        const monthly = thp * 0.2;
        const r = 0.06 / 12;
        let html = '';
        for (let y = 1; y <= 5; y++) {
          const months = y * 12;
          const pokok = monthly * months;
          const fv = monthly * ((Math.pow(1 + r, months) - 1) / r);
          html += `<tr><td>Tahun ${y}</td><td class="right">${HC.fmt(pokok)}</td><td class="right" style="color:var(--green-dark);font-weight:700">${HC.fmt(fv)}</td></tr>`;
        }
        $('proyeksiBody').innerHTML = html;

        lastCalc = { gaji, tunj, tunjVar, ptkpLabel: els.ptkp.options[els.ptkp.selectedIndex].text, potLain, brutoBulan, bpjsTotal, pphBulan, thp, thpPct };
        $('placeholder').style.display = 'none';
        $('results').classList.add('show');
        $('results').scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        HC.toast('Gaji bersih berhasil dihitung!');
      }

      function reset() {
        els.gaji.value = '8.000.000'; els.tunj.value = '1.500.000'; els.tunjVar.value = '0';
        els.ptkp.value = '54000000'; els.potLain.value = '0';
        $('placeholder').style.display = ''; $('results').classList.remove('show');
        lastCalc = null; HC.toast('Form direset', 'info');
      }

      function buildReport() {
        if (!lastCalc) return null;
        const c = lastCalc;
        const insight = c.thpPct >= 90
          ? `Take-Home Pay ${c.thpPct.toFixed(1)}% dari bruto — sangat efisien! Maksimalkan alokasi 20% untuk tabungan & investasi.`
          : `Take-Home Pay ${c.thpPct.toFixed(1)}% dari bruto. PPh 21 cukup besar — pertimbangkan zakat via lembaga resmi untuk kurangi PKP.`;

        return HC.buildReportCard({
          title: '💰 Gaji Bersih (Take-Home Pay)',
          color: '#059669', color2: '#047857',
          heroLabel: 'Take-Home Pay/Bulan',
          heroValue: HC.fmt(c.thp),
          heroSubtext: `${c.thpPct.toFixed(1)}% dari bruto · ${c.ptkpLabel}`,
          inputs: [
            { label: '💼 Gaji Pokok', value: HC.fmt(c.gaji) },
            { label: '➕ Tunjangan Tetap', value: HC.fmt(c.tunj) },
            { label: '⚡ Tunj. Tidak Tetap', value: HC.fmt(c.tunjVar) },
            { label: '👨‍👩‍👧 Status PTKP', value: c.ptkpLabel },
          ],
          results: [
            { label: 'Take-Home Pay', value: HC.fmt(c.thp), highlight: true },
            { label: 'Bruto/Bulan', value: HC.fmt(c.brutoBulan) },
            { label: 'BPJS Karyawan (4%)', value: HC.fmt(c.bpjsTotal) },
            { label: 'PPh 21/Bulan', value: HC.fmt(c.pphBulan) },
            { label: 'Untuk Kebutuhan (50%)', value: HC.fmt(c.thp * 0.5) },
            { label: 'Untuk Tabungan (20%)', value: HC.fmt(c.thp * 0.2), highlight: true },
          ],
          insight, slug: 'gaji-bersih',
        });
      }

      function summaryText() {
        if (!lastCalc) return '';
        const c = lastCalc;
        return `💰 *Gaji Bersih - HitungCerdas*\n\nBruto: ${HC.fmt(c.brutoBulan)}/bln\nBPJS: ${HC.fmt(c.bpjsTotal)}\nPPh 21: ${HC.fmt(c.pphBulan)}\n\n💵 *Take-Home Pay: ${HC.fmt(c.thp)}/bulan*\n📊 ${c.thpPct.toFixed(1)}% dari bruto\n\nAlokasi 50/30/20:\n• Kebutuhan: ${HC.fmt(c.thp * 0.5)}\n• Keinginan: ${HC.fmt(c.thp * 0.3)}\n• Tabungan: ${HC.fmt(c.thp * 0.2)}\n\nHitung di: https://hitungcerdas.net/kalkulator/gaji-bersih`;
      }

      $('btnHitung').addEventListener('click', hitung);
      $('btnReset').addEventListener('click', reset);
      $('exportImg').addEventListener('click', () => { const c = buildReport(); if (!c) { HC.toast('Hitung dulu', 'error'); return; } HC.exportToImage(c, 'gaji-bersih-' + Date.now()); });
      $('exportWa').addEventListener('click', () => { const c = buildReport(); if (!c) { HC.toast('Hitung dulu', 'error'); return; } HC.shareWhatsApp(c, summaryText(), 'gaji-bersih'); });
      $('exportPdf').addEventListener('click', () => { if (!lastCalc) { HC.toast('Hitung dulu', 'error'); return; } HC.exportToPDF(); });
      $('exportCopy').addEventListener('click', () => { if (!lastCalc) { HC.toast('Hitung dulu', 'error'); return; } HC.copyText(summaryText(), 'Tersalin!'); });
    });
  })();
</script>

<script is:inline>
  (function(){
    var LIMIT = 5;
    function ready(fn){ if(window.HC){ fn(); } else { setTimeout(function(){ready(fn);},120); } }
    function $(id){ return document.getElementById(id); }
    ready(function(){
      var wrap=$('aicWrap'); if(!wrap) return;
      var trigger=$('aicTrigger'), panel=$('aicPanel'), ground=$('aicGround'), msgs=$('aicMsgs'), chips=$('aicChips');
      var input=$('aicText'), send=$('aicSend'), limitEl=$('aicLimit'); var history=[];
      function isDark(){ var de=document.documentElement; return de.classList.contains('dark')||de.getAttribute('data-theme')==='dark'||(function(){try{return localStorage.getItem('hc-theme')==='dark';}catch(e){return false;}})(); }
      function applyTheme(){ wrap.classList.toggle('aic-dark', isDark()); }
      applyTheme();
      try{ new MutationObserver(applyTheme).observe(document.documentElement,{attributes:true,attributeFilter:['class','data-theme']}); }catch(e){}
      window.addEventListener('storage', function(e){ if(e.key==='hc-theme') applyTheme(); });
      function num(v){ try{ return HC.parseRp(String(v||'')); }catch(e){ return 0; } }
      function selText(id){ var x=$(id); return (x&&x.options&&x.selectedIndex>=0)?x.options[x.selectedIndex].text:''; }
      function esc(t){ return String(t).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }


      function readData(){
        var thpTxt=($('thp')||{}).textContent||'Rp 0';
        var brutoTxt=($('bruto')||{}).textContent||'Rp 0';
        var bpjsTxt=($('bpjsTotal')||{}).textContent||'Rp 0';
        var pphTxt=($('pphBulan')||{}).textContent||'Rp 0';
        var pct=parseFloat(String(($('thpPct')||{}).textContent||'').replace(',','.')); if(!isFinite(pct)) pct=null;
        return { thpTxt:thpTxt, thpNum:num(thpTxt), brutoTxt:brutoTxt, brutoNum:num(brutoTxt),
          bpjsTxt:bpjsTxt, pphTxt:pphTxt, pct:pct,
          gaji:($('gaji')||{}).value||'', tunj:($('tunj')||{}).value||'', tunjVar:($('tunjVar')||{}).value||'',
          ptkp:selText('ptkp'), potLain:($('potLain')||{}).value||'' };
      }

      function renderGround(){
        var d=readData();
        var ctx='<div class="aic-ctx"><i class="ti ti-wallet"></i> Gaji Bersih \u00b7 Take-home <b>'+esc(d.thpTxt)+'</b></div>';
        if(!d.brutoNum || !d.thpNum){ ground.innerHTML=ctx+'<div class="aic-nogauge">Hitung dulu untuk lihat komposisi gaji yang sampai ke kantong.</div>'; return; }
        var pct=d.pct!=null?d.pct:(d.thpNum/d.brutoNum*100); pct=Math.max(0,Math.min(100,pct));
        var cut=Math.max(0,100-pct); var potTxt=HC.fmt(Math.max(0,d.brutoNum-d.thpNum));
        var vc=pct>=85?'good':(pct>=75?'warn':'bad');
        var vi=pct>=85?'ti-circle-check':'ti-alert-triangle';
        var vt=pct>=85?('Efisien \u2014 '+pct.toFixed(0)+'% gajimu sampai ke kantong'):('Potongan cukup besar \u2014 hanya '+pct.toFixed(0)+'% yang jadi take-home');
        ground.innerHTML=ctx
          +'<div class="aic-stack"><div class="seg s-green" style="width:'+pct.toFixed(1)+'%"><span class="seg-lbl">Diterima</span></div><div class="seg s-amber" style="width:'+cut.toFixed(1)+'%"><span class="seg-lbl">Potongan</span></div></div>'
          +'<div class="aic-legend"><span><span class="dot" style="background:#10B981"></span>Diterima: <b>'+esc(d.thpTxt)+'</b></span><span><span class="dot" style="background:#FDE68A"></span>Potongan: <b>'+esc(potTxt)+'</b></span></div>'
          +'<div class="aic-verdict '+vc+'"><i class="ti '+vi+'"></i> '+vt+'</div>';
      }

      function buildContext(){
        var d=readData();
        var pct=d.pct!=null?d.pct:(d.brutoNum?d.thpNum/d.brutoNum*100:null);
        var p=[
          'Kalkulator: Gaji Bersih (take-home pay setelah BPJS dan PPh 21).',
          d.gaji?('Gaji pokok: Rp '+d.gaji+'.'):'',
          (d.tunj && d.tunj!=='0')?('Tunjangan tetap: Rp '+d.tunj+'.'):'',
          (d.tunjVar && d.tunjVar!=='0')?('Tunjangan tidak tetap (lembur/komisi): Rp '+d.tunjVar+'.'):'',
          d.ptkp?('Status PTKP: '+d.ptkp+'.'):'',
          'Gaji bruto per bulan: '+d.brutoTxt+'.',
          'Potongan BPJS karyawan: '+d.bpjsTxt+'. PPh 21 per bulan: '+d.pphTxt+'.',
          (d.potLain && d.potLain!=='0')?('Potongan lain per bulan: Rp '+d.potLain+'.'):'',
          'Take-home pay (gaji bersih) per bulan: '+d.thpTxt+(pct!=null?(' ('+pct.toFixed(1)+'% dari bruto)'):'')+'.',
          'Acuan: alokasi sehat 50% kebutuhan, 30% keinginan, 20% tabungan/investasi. Take-home pay umumnya 70-85% dari bruto.'
        ];
        return p.filter(Boolean).join('\n');
      }

      function mdInline(t){ t=t.replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>'); t=t.replace(/__([^_]+)__/g,'<strong>$1</strong>'); t=t.replace(/(^|[\s(])\*([^*\n]+)\*(?=[\s).,!?:;]|$)/g,'$1<em>$2</em>'); t=t.replace(/`([^`]+)`/g,'<code>$1</code>'); return t; }
      function mdToHtml(raw){ var lines=String(raw).replace(/\r/g,'').split('\n'); var out=[]; for(var i=0;i<lines.length;i++){ var t=lines[i].trim(); if(t==='')continue; if(/^([-*_]\s*){3,}$/.test(t))continue; var h=t.match(/^#{1,6}\s+(.*)$/); if(h){out.push('<p class="aic-h">'+mdInline(esc(h[1]))+'</p>');continue;} var b=t.match(/^[-*\u2022]\s+(.*)$/); if(b){out.push('<div class="aic-li"><span class="aic-mk">\u2022</span><span>'+mdInline(esc(b[1]))+'</span></div>');continue;} var n=t.match(/^(\d+)[.)]\s+(.*)$/); if(n){out.push('<div class="aic-li"><span class="aic-mk">'+n[1]+'.</span><span>'+mdInline(esc(n[2]))+'</span></div>');continue;} out.push('<p>'+mdInline(esc(t))+'</p>'); } return out.join('')||('<p>'+mdInline(esc(String(raw)))+'</p>'); }
      function addMsg(role,text){ var row=document.createElement('div'); row.className='aic-row '+role; if(role==='ai'){ row.innerHTML='<div class="aic-av"><i class="ti ti-sparkles"></i></div><div class="aic-bub aic-rich"></div>'; row.querySelector('.aic-bub').innerHTML=mdToHtml(text); } else { row.innerHTML='<div class="aic-bub"></div>'; row.querySelector('.aic-bub').textContent=text; } msgs.appendChild(row); msgs.scrollTop=msgs.scrollHeight; return row; }
      function addTyping(){ var row=document.createElement('div'); row.className='aic-row ai'; row.innerHTML='<div class="aic-av"><i class="ti ti-sparkles"></i></div><div class="aic-bub aic-typing"><span></span><span></span><span></span></div>'; msgs.appendChild(row); msgs.scrollTop=msgs.scrollHeight; return row; }
      function todayKey(){ var d=new Date(); return 'hc-coach-'+d.getFullYear()+'-'+(d.getMonth()+1)+'-'+d.getDate(); }
      function getCount(){ try{ return parseInt(localStorage.getItem(todayKey())||'0',10)||0; }catch(e){ return 0; } }
      function incCount(){ try{ localStorage.setItem(todayKey(), getCount()+1); }catch(e){} }
      function refreshLimit(){ var left=Math.max(0,LIMIT-getCount()); limitEl.textContent='Sisa '+left+' pertanyaan gratis hari ini.'; var done=left<=0; input.disabled=done; send.disabled=done; if(done){ input.placeholder='Batas harian tercapai, balik lagi besok ya.'; } }
      var busy=false;
      async function ask(text){ text=(text||'').trim(); if(!text||busy)return; if(getCount()>=LIMIT){refreshLimit();return;} busy=true; send.disabled=true; addMsg('user',text); history.push({role:'user',content:text}); input.value=''; chips.innerHTML=''; var typing=addTyping(); incCount();
        try{ var res=await fetch('/api/coach',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({calcContext:buildContext(),messages:history})}); var data=await res.json().catch(function(){return {error:'Respon tidak valid.'};}); typing.parentNode&&typing.remove(); if(data&&data.reply){ addMsg('ai',data.reply); history.push({role:'assistant',content:data.reply}); } else { addMsg('ai','\u26a0\ufe0f '+((data&&data.error)||'Coach lagi sibuk, coba lagi sebentar ya.')); } }
        catch(e){ typing.parentNode&&typing.remove(); addMsg('ai','\u26a0\ufe0f Gagal terhubung ke Coach. Coba lagi ya.'); }
        busy=false; refreshLimit(); if(!input.disabled) input.focus(); }
      var PRESETS=['Take-home segini wajar gak?','Cara atur 50/30/20 dari gaji ini?','Kenapa potongannya segini?'];
      function renderChips(){ chips.innerHTML=''; PRESETS.forEach(function(q){ var b=document.createElement('button'); b.type='button'; b.className='aic-chip'; b.textContent=q; b.addEventListener('click',function(){ask(q);}); chips.appendChild(b); }); }
      var opened=false;
      trigger.addEventListener('click', function(){ if(panel.hasAttribute('hidden')){ panel.removeAttribute('hidden'); wrap.classList.add('open'); applyTheme(); renderGround(); refreshLimit(); if(!opened){renderChips();opened=true;} } else { panel.setAttribute('hidden',''); wrap.classList.remove('open'); } });
      send.addEventListener('click', function(){ ask(input.value); });
      input.addEventListener('keydown', function(e){ if(e.key==='Enter'){ e.preventDefault(); ask(input.value); } });
      var bh=$('btnHitung'); if(bh){ bh.addEventListener('click', function(){ setTimeout(function(){ if(!panel.hasAttribute('hidden')) renderGround(); },60); }); }
    });
  })();
</script>
